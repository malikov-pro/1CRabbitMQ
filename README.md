# 1CRabbitMQ — Внешняя компонента RabbitMQ для 1C

> **Нативная компонента на Rust/C++** для удобного взаимодействия с брокером сообщений **RabbitMQ** без внешних прокси-сервисов.

[![Platform](https://img.shields.io/badge/1C-8.3.21+-blue.svg)](https://releases.1c.ru/)
[![OS](https://img.shields.io/badge/OS-Windows%20%7C%20Linux-green.svg)]()
[![License](https://img.shields.io/badge/License-Proprietary-red.svg)]()

---

## 📋 Содержание

1. [Обзор](#-обзор)
2. [Возможности](#-возможности)
3. [Требования](#-требования)
4. [Установка](#-установка)
5. [Быстрый старт](#-быстрый-старт)
6. [Основные концепции](#-основные-концепции)
7. [API Reference](#-api-reference)
8. [Паттерны использования](#-паттерны-использования)
9. [Продвинутые возможности](#-продвинутые-возможности)
10. [Best Practices](#-best-practices)
11. [Troubleshooting](#-troubleshooting)
12. [Ссылки и контакты](#-ссылки-и-контакты)

---

## 🎯 Обзор

**1CRabbitMQ** — это нативная внешняя компонента для работы с брокером сообщений RabbitMQ из среды 1С:Предприятие. Компонента написана на **Rust** (основная логика) и **C++** (интерфейс для 1С), что обеспечивает:

- ⚡ **Высокую производительность** — сопоставимую с приложениями на C++
- 🛡️ **Безопасность памяти** — минимальный риск утечек благодаря Rust
- 🔌 **Простоту использования** — через обработку `КлиентRMQ`
- 🌐 **Кросс-платформенность** — Windows и Linux x64

Компонента работает **напрямую** с RabbitMQ по протоколу AMQP 0.9.1, без необходимости разворачивать промежуточные веб-сервисы или REST API.

**Текущая версия:** 1.0.0 (обновлена 12.08.2024, последнее исправление 23.11.2025)  
**Базовая библиотека:** [lapin 3.2.0](https://github.com/amqp-rs/lapin) — современная async AMQP библиотека на Rust с множеством оптимизаций

---

## ✨ Возможности

### Базовые операции
- ✅ Подключение к серверу RabbitMQ
- ✅ Создание и управление каналами
- ✅ Объявление очередей и привязка к обменникам
- ✅ Публикация сообщений
- ✅ Получение сообщений (блокирующий и неблокирующий режимы)
- ✅ Подтверждение и отклонение сообщений

### Продвинутые возможности
- ✅ **Publisher Confirms** — подтверждение доставки от сервера
- ✅ **QoS (prefetch_count)** — контроль количества неподтвержденных сообщений
- ✅ **Message Properties** — `reply_to`, `correlation_id`, `expiration` и др.
- ✅ **Custom Headers** — произвольные заголовки сообщений
- ✅ **Message Compression** — сжатие gzip и zstd
- ✅ **Stream Queues** — поддержка RabbitMQ Streams (3.9+)
- ✅ **Dead Letter Exchanges** — обработка "мертвых" сообщений
- ✅ **Durable Queues & Persistent Messages** — устойчивость к перезагрузкам

### Режимы работы
- 🖥️ **Файловый и серверный варианты** 1С
- 🔄 **Синхронная и асинхронная обработка** сообщений
- ⚙️ **Регламентные задания** для фоновой обработки
- 📊 **Мониторинг и логирование** работы компоненты

---

## 📌 Требования

### Платформа 1С:Предприятие
- **Windows:** версия **8.3.21 и выше** (требование работы в изолированном режиме)
  - **Важно для Windows:** Компонента работает в **изолированном режиме** — в отдельном процессе `addnhost64` (не в `rphost`)
  - Это обеспечивает безопасность: в случае ошибки в компоненте не произойдет падения основного процесса `rphost`
  - Версии ниже 8.3.21 не поддерживают изолированный режим для внешних компонент
- **Linux:** ограничений на версию нет
  - Компонента работает стабильно на любых версиях 1С для Linux
- **Разрядность:** только **x86-64** (32-битные системы не поддерживаются)
- **⚠️ КРИТИЧНО:** Компонента работает **только на сервере**. Все вызовы API должны выполняться в серверном контексте (`&НаСервере`, регламентные задания, фоновые задания). Вызовы из клиентского контекста (`&НаКлиенте`) невозможны.

### Дополнительные зависимости (Windows)
- **Microsoft Visual C++ Redistributable** (обычно уже установлен)
  - Скачать: https://aka.ms/vs/17/release/vc_redist.x64.exe

### RabbitMQ
- Версия **3.8+** (рекомендуется 3.12+)
- Поддержка протокола **AMQP 0.9.1**

---

## 🚀 Установка

### 1. Получение компоненты

Компонента распространяется в виде конфигурации 1С с примерами.

- **Скачать:** [Releases](https://github.com/kovalevdmv/1CRabbitMQ/releases)
- **Бесплатная версия:** ограничение на размер сообщения — 10'000 символов
- **Полная версия:** без ограничений, приобретается у автора

### 2. Установка в конфигурацию

1. Откройте конфигурацию 1С в режиме Конфигуратора
2. Скопируйте обработку **`КлиентRMQ`** из демо-конфигурации в вашу
3. Обработка включает в себя:
   - Макет `windows` — бинарный файл компоненты для Windows
   - Макет `linux` — бинарный файл компоненты для Linux
   - Объектный модуль — обертка над компонентой (API)

### 3. Проверка установки

Создайте простой тест:

```bsl
КлиентRMQ = Обработки.КлиентRMQ.Создать();
Результат = КлиентRMQ.ПодключитьКомпоненту();

Если КлиентRMQ.ЭтоОшибка(Результат) Тогда
    Сообщить("Ошибка: " + Результат.Текст);
Иначе
    Сообщить("Компонента успешно подключена!");
КонецЕсли;
```

---

## ⚡ Быстрый старт

### Шаг 1: Запуск RabbitMQ (Docker)

Самый простой способ — использовать Docker. Подробные инструкции смотрите в [Запуск сервиса RMQ в Docker](./lessons/DOCKER_README.md).

### Шаг 2: Отправка сообщения

```bsl
// ⚠️ Весь код выполняется НА СЕРВЕРЕ!

&НаСервере
Процедура ОтправитьСообщениеНаСервере()
    // Создать клиента
    КлиентRMQ = Обработки.КлиентRMQ.Создать();
    
    // Подключиться к серверу
    СтрокаПодключения = "amqp://rmuser:rmpassword@localhost:5672/%2f";
    Подключение = КлиентRMQ.ПодключитьсяКСерверу(СтрокаПодключения);
    
    Если КлиентRMQ.ЭтоОшибка(Подключение) Тогда
        Сообщить("Ошибка подключения: " + Подключение.Текст);
        Возврат;
    КонецЕсли;
    
    // Создать канал
    Канал = КлиентRMQ.СоздатьКанал(Подключение);
    
    // Объявить очередь
    КлиентRMQ.ОбъявитьОчередь(Канал, "hello");
    
    // Отправить сообщение
    Ответ = КлиентRMQ.ОпубликоватьСообщение(
        Канал, 
        "Hello, RabbitMQ!", 
        "hello"  // имя очереди = ключ маршрутизации
    );
    
    Сообщить("Сообщение отправлено!");
    
    // Отключиться
    КлиентRMQ.ОтключитьсяОтСервера(Подключение);
КонецПроцедуры

// Клиентская кнопка вызывает серверную процедуру
&НаКлиенте
Процедура ОтправитьСообщение(Команда)
    ОтправитьСообщениеНаСервере();
КонецПроцедуры
```

### Шаг 3: Получение сообщения

```bsl
// ⚠️ Получение сообщений ДОЛЖНО выполняться в фоновом/регламентном задании!

&НаСервере
Процедура ПолучитьСообщениеНаСервере()
    КлиентRMQ = Обработки.КлиентRMQ.Создать();
    Подключение = КлиентRMQ.ПодключитьсяКСерверу("amqp://rmuser:rmpassword@localhost:5672/%2f");
    Канал = КлиентRMQ.СоздатьКанал(Подключение);
    
    // Получить одно сообщение (без блокировки)
    Если КлиентRMQ.ОдноСледующееСообщение(Канал, "hello") Тогда
        ДанныеСообщения = КлиентRMQ.ДанныеСообщения();
        Сообщить("Получено: " + ДанныеСообщения.Данные);
        
        // Подтвердить получение
        КлиентRMQ.ПодтвердитьСообщение(Канал, ДанныеСообщения.Тег);
    КонецЕсли;
    
    КлиентRMQ.ОтключитьсяОтСервера(Подключение);
КонецПроцедуры
```

> **⚠️ ВАЖНО:** Для постоянного прослушивания очереди (блокирующий цикл `СледующееСообщение()`) используйте **регламентное задание** или **фоновое задание**, а не вызовы из формы!

---

## 📚 Основные концепции

### Иерархия объектов

```
Подключение (Connection)
  └─ Канал (Channel)
       ├─ Очередь (Queue)
       ├─ Получатель (Consumer)
       └─ Публикация (Publish)
```

### Подключение (Connection)
- Физическое TCP-соединение с сервером RabbitMQ
- Одно подключение может содержать множество каналов
- Создается через `ПодключитьсяКСерверу()`

### Канал (Channel)
- "Виртуальное" соединение внутри подключения
- Легковесная альтернатива множественным TCP-соединениям
- Все операции (публикация, чтение) выполняются в рамках канала
- Создается через `СоздатьКанал()`

### Очередь (Queue)
- Буфер для хранения сообщений
- Сообщения хранятся до подтверждения получения
- Может быть durable (переживает перезагрузку сервера)
- Объявляется через `ОбъявитьОчередь()`

### Обменник (Exchange)
- Маршрутизирует сообщения в очереди по правилам
- Типы: `direct`, `fanout`, `topic`, `headers`
- **Особенность компоненты:** нет явного метода объявления, используются предустановленные или создаются через веб-интерфейс

### Привязка (Binding)
- Связь между обменником и очередью
- Создается неявно через параметры `ОбъявитьОчередь()`

---

## 📖 API Reference

[Полная документация API](./API_Documentation.md)

---

## 🎨 Паттерны использования

### 1. Simple Queue (Hello World)

**Применение:** Простая отправка задач между системами.

```bsl
// Отправитель
КлиентRMQ.ОпубликоватьСообщение(Канал, "Task #1", "tasks");

// Получатель
Если КлиентRMQ.ОдноСледующееСообщение(Канал, "tasks") Тогда
    Сообщение = КлиентRMQ.ДанныеСообщения();
    // Обработка...
КонецЕсли;
```

---

### 2. Work Queues (Очереди задач)

**Применение:** Распределение тяжелых задач между несколькими воркерами.

**Ключевые настройки:**
- Durable queue + Persistent messages
- Manual acknowledgment
- QoS = 1 (справедливое распределение)

```bsl
// Отправитель
Канал = КлиентRMQ.СоздатьКанал(Подключение);
КлиентRMQ.ОбъявитьОчередь(Канал, "task_queue", "", "", Истина); // durable
КлиентRMQ.ОпубликоватьСообщение(Канал, "Heavy Task", "task_queue", "", Истина, Истина); // persistent

// Воркер
Канал = КлиентRMQ.СоздатьКанал(Подключение, Ложь, 1); // QoS = 1
Получатель = КлиентRMQ.СоздатьПолучателя(Канал, "task_queue", "", Ложь); // no_ack = Ложь

Пока КлиентRMQ.СледующееСообщение(Получатель) Цикл
    Данные = КлиентRMQ.ДанныеСообщения();
    
    Попытка
        // Обработка...
        Сообщить("Обработка: " + Данные.Данные);
        
        // Подтвердить
        КлиентRMQ.ПодтвердитьСообщение(Канал, Данные.Тег);
    Исключение
        // Отклонить и вернуть в очередь
        КлиентRMQ.ОтклонитьСообщение(Канал, Данные.Тег, Истина);
    КонецПопытки;
КонецЦикла;
```

---

### 3. Publish/Subscribe (Pub/Sub)

**Применение:** Рассылка уведомлений множеству подписчиков.

**Обменник:** `fanout` (доставляет сообщение всем привязанным очередям)

```bsl
// Издатель
КлиентRMQ.ОпубликоватьСообщение(Канал, "Event happened!", "", КлиентRMQ.Exchange_fanout());

// Подписчик 1
Очередь1 = КлиентRMQ.ОбъявитьОчередь(Канал, "", "", КлиентRMQ.Exchange_fanout(), Ложь, Истина, Истина);
Получатель1 = КлиентRMQ.СоздатьПолучателя(Канал, Очередь1.ИмяОчереди);

// Подписчик 2
Очередь2 = КлиентRMQ.ОбъявитьОчередь(Канал, "", "", КлиентRMQ.Exchange_fanout(), Ложь, Истина, Истина);
Получатель2 = КлиентRMQ.СоздатьПолучателя(Канал, Очередь2.ИмяОчереди);
```

---

### 4. Routing (Маршрутизация)

**Применение:** Селективная доставка сообщений (например, логи по уровням).

**Обменник:** `direct` (точное совпадение ключа маршрутизации)

```bsl
// Издатель
КлиентRMQ.ОпубликоватьСообщение(Канал, "Critical error!", "error", "logs_direct");
КлиентRMQ.ОпубликоватьСообщение(Канал, "Info message", "info", "logs_direct");

// Подписчик на ошибки
ОчередьОшибок = КлиентRMQ.ОбъявитьОчередь(Канал, "", "error", "logs_direct", Ложь, Истина, Истина);

// Подписчик на все
ОчередьВсе = КлиентRMQ.ОбъявитьОчередь(Канал, "", "", "logs_direct", Ложь, Истина, Истина);
КлиентRMQ.ОбъявитьОчередь(Канал, ОчередьВсе.ИмяОчереди, "error", "logs_direct");
КлиентRMQ.ОбъявитьОчередь(Канал, ОчередьВсе.ИмяОчереди, "info", "logs_direct");
КлиентRMQ.ОбъявитьОчередь(Канал, ОчередьВсе.ИмяОчереди, "warning", "logs_direct");
```

---

### 5. Topics (Шаблоны маршрутизации)

**Применение:** Гибкая маршрутизация с использованием шаблонов.

**Обменник:** `topic` (поддерживает `*` и `#` в binding keys)

- `*` — одно слово
- `#` — ноль или более слов

```bsl
// Издатель
КлиентRMQ.ОпубликоватьСообщение(Канал, "Склад, поступление, срочно", "склад.поступление.срочно", "logs_topic");
КлиентRMQ.ОпубликоватьСообщение(Канал, "Склад, отгрузка, обычно", "склад.отгрузка.обычно", "logs_topic");
КлиентRMQ.ОпубликоватьСообщение(Канал, "Документы, ошибка", "документы.ошибка", "logs_topic");

// Подписчик: все события склада
Очередь1 = КлиентRMQ.ОбъявитьОчередь(Канал, "", "склад.#", "logs_topic", Ложь, Истина, Истина);

// Подписчик: все срочные события
Очередь2 = КлиентRMQ.ОбъявитьОчередь(Канал, "", "*.*.срочно", "logs_topic", Ложь, Истина, Истина);

// Подписчик: все ошибки
Очередь3 = КлиентRMQ.ОбъявитьОчередь(Канал, "", "*.ошибка", "logs_topic", Ложь, Истина, Истина);
```

---

### 6. RPC (Remote Procedure Call)

**Применение:** Синхронный вызов удаленных функций.

**Ключевые компоненты:**
- `reply_to` — имя очереди для ответа
- `correlation_id` — связь запроса и ответа

```bsl
// RPC-клиент
ОчередьОтветов = КлиентRMQ.ОбъявитьОчередь(Канал, "", "", "", Ложь, Истина, Истина);
correlation_id = Строка(Новый УникальныйИдентификатор());

Свойства = КлиентRMQ.НовыеСвойстваСообщения();
Свойства.reply_to = ОчередьОтветов.ИмяОчереди;
Свойства.correlation_id = correlation_id;

КлиентRMQ.ОпубликоватьСообщение(Канал, "30", "rpc_queue", "", Истина, Ложь, Свойства);

// Ожидание ответа
Пока Истина Цикл
    Если КлиентRMQ.ОдноСледующееСообщение(Канал, ОчередьОтветов.ИмяОчереди) Тогда
        Ответ = КлиентRMQ.ДанныеСообщения();
        Если Ответ.СвойстваСообщения.correlation_id = correlation_id Тогда
            Результат = Ответ.Данные;
            Прервать;
        КонецЕсли;
    КонецЕсли;
КонецЦикла;

// RPC-сервер (регламентное задание)
Получатель = КлиентRMQ.СоздатьПолучателя(Канал, "rpc_queue", "", Ложь);
Пока КлиентRMQ.СледующееСообщение(Получатель) Цикл
    Запрос = КлиентRMQ.ДанныеСообщения();
    
    // Вычисление (например, Фибоначчи)
    Результат = ВычислитьФибоначчи(Число(Запрос.Данные));
    
    // Отправка ответа
    ОтветныеСвойства = КлиентRMQ.НовыеСвойстваСообщения();
    ОтветныеСвойства.correlation_id = Запрос.СвойстваСообщения.correlation_id;
    КлиентRMQ.ОпубликоватьСообщение(Канал, Строка(Результат), Запрос.СвойстваСообщения.reply_to, "", Истина, Ложь, ОтветныеСвойства);
    
    КлиентRMQ.ПодтвердитьСообщение(Канал, Запрос.Тег);
КонецЦикла;
```

---

## 🚀 Продвинутые возможности

### Publisher Confirms

Гарантирует, что сообщение было принято брокером.

```bsl
// Включить режим confirm
Канал = КлиентRMQ.СоздатьКанал(Подключение, Истина);

// Отправить
Ответ = КлиентRMQ.ОпубликоватьСообщение(Канал, "Critical data", "important_queue");

// Проверить подтверждение
Если Ответ.Текст = КлиентRMQ.ПодтверждениеСервера_СерверПодтвердилПолучениеСообщения() Тогда
    Сообщить("✓ Доставка подтверждена");
ИначеЕсли Ответ.Текст = КлиентRMQ.ПодтверждениеСервера_Сервер_НЕ_ПодтвердилПолучениеСообщения() Тогда
    ВызватьИсключение "✗ Ошибка доставки!";
КонецЕсли;
```

> **⚠️ Ограничение:** Поддерживается только синхронный режим (сообщение за сообщением). Асинхронные подтверждения не реализованы.

---

### Сжатие сообщений

Поддерживается сжатие больших сообщений с автоматической распаковкой на стороне получателя.

**Алгоритмы:**
- **zstd** (рекомендуется) — современный алгоритм от Facebook
  - Уровни: 1-22
  - Рекомендуемый уровень: **22** (максимальный, т.к. сжимает быстро, а коэффициент значительно лучше)
  - Оптимальное соотношение скорости и степени сжатия
- **gzip** (устаревший, не рекомендуется)
  - Уровни: 1-9
  - Рекомендуемый уровень: **4** (оптимально, т.к. макс. уровень 9 сжимает плохо и медленно)
  - Менее эффективен, чем zstd

**Важно:** При сжатии компонента автоматически устанавливает свойство `content_encoding` в значение алгоритма сжатия. Получатель автоматически распаковывает сообщение на основе этого свойства.

```bsl
// Настройки сжатия
НастройкиСжатия = КлиентRMQ.НастройкиСжатия(
    КлиентRMQ.АлгоритмСжатия_zstd(),  // "zstd" (рекомендуется) или "gzip"
    22                                 // уровень 22 для zstd (максимальный)
);

// Отправка со сжатием
КлиентRMQ.ОпубликоватьСообщение(
    Канал, 
    ОченьБольшиеДанные,  // строка > 100 KB
    "big_data", 
    "", 
    Истина, 
    Истина, 
    Неопределено, 
    Неопределено, 
    НастройкиСжатия
);

// Получение - распаковка автоматическая!
Если КлиентRMQ.СледующееСообщение(Получатель) Тогда
    ДанныеСообщения = КлиентRMQ.ДанныеСообщения();
    // Данные уже распакованы, если было сжатие
    Сообщить(ДанныеСообщения.Данные);
КонецЕсли;
```

**Рекомендации по использованию:**
- Используйте сжатие для сообщений **> 10 KB**
- Предпочитайте **zstd уровень 22** для максимальной эффективности
- Не используйте gzip (только для совместимости со старыми системами)
- Бесплатная версия: учитывайте лимит 10'000 символов **до** сжатия

---

### Stream Queues

Специальный тип очередей для потоковой обработки (RabbitMQ 3.9+).

```bsl
// Создание Stream очереди
Аргументы = Новый Массив;
Аргументы.Добавить(КлиентRMQ.НовыйАргумент("x-queue-type", "stream", "строка"));

Очередь = КлиентRMQ.ОбъявитьОчередь(Канал, "my_stream", "", "", Истина, Ложь, Ложь, Аргументы);

// QoS обязателен для Stream
Канал = КлиентRMQ.СоздатьКанал(Подключение, Ложь, 100);
```

---

### Dead Letter Exchanges (DLX)

Перенаправление необработанных сообщений.

```bsl
// Создание DLX-очереди
АргументыDLX = Новый Массив;
АргументыDLX.Добавить(КлиентRMQ.НовыйАргумент("x-dead-letter-exchange", "dlx", "строка"));
АргументыDLX.Добавить(КлиентRMQ.НовыйАргумент("x-dead-letter-routing-key", "failed", "строка"));

КлиентRMQ.ОбъявитьОчередь(Канал, "tasks_with_dlx", "", "", Истина, Ложь, Ложь, АргументыDLX);

// При отклонении без requeue сообщение попадет в DLX
КлиентRMQ.ОтклонитьСообщение(Канал, ДанныеСообщения.Тег, Ложь);
```

---

### TTL (Time-To-Live)

Автоматическое удаление или перенаправление истекших сообщений.

```bsl
// TTL для всей очереди
АргументыTTL = Новый Массив;
АргументыTTL.Добавить(КлиентRMQ.НовыйАргумент("x-message-ttl", 60000, "число")); // 60 секунд

КлиентRMQ.ОбъявитьОчередь(Канал, "temp_queue", "", "", Ложь, Ложь, Ложь, АргументыTTL);

// TTL для конкретного сообщения
Свойства = КлиентRMQ.НовыеСвойстваСообщения();
Свойства.expiration = "30000"; // 30 секунд (СТРОКА!)

КлиентRMQ.ОпубликоватьСообщение(Канал, "Temporary message", "temp_queue", "", Истина, Ложь, Свойства);
```

---

## 💡 Best Practices

### 1. Управление соединениями

Компонента автоматически определяет режим работы 1С (файловый или серверный) и соответственно управляет подключениями:

- **Файловый вариант:** Компонента работает в режиме `Mode = 0`
  - При завершении работы компоненты (метод `Done`) завершается асинхронный рантайм Rust
  - Без этого будет аварийное завершение
  - Рекомендуется: создавать и закрывать соединение в каждой операции
  
- **Серверный вариант:** Компонента работает в режиме `Mode = 1`
  - Используются общие подключения для мониторинга и управления из других сессий
  - Рекомендуется: переиспользовать соединения между операциями

#### ✅ Правильно:

```bsl
// Файловый вариант: создавать/закрывать соединение в каждой операции
&НаСервере
Процедура ОтправитьСообщение(Данные)
    КлиентRMQ = Обработки.КлиентRMQ.Создать();
    Подключение = КлиентRMQ.ПодключитьсяКСерверу(СтрокаПодключения);
    Канал = КлиентRMQ.СоздатьКанал(Подключение);
    
    // Операции...
    
    КлиентRMQ.ОтключитьсяОтСервера(Подключение);
КонецПроцедуры

// Серверный вариант: переиспользовать соединение
Перем ОбщееПодключение;

&НаСервере
Процедура ПриСозданииНаСервере()
    КлиентRMQ = Обработки.КлиентRMQ.Создать();
    ОбщееПодключение = КлиентRMQ.ПодключитьсяКСерверу(СтрокаПодключения);
КонецПроцедуры

&НаСервере
Процедура ОтправитьСообщение(Данные)
    Канал = КлиентRMQ.СоздатьКанал(ОбщееПодключение);
    // Операции...
    КлиентRMQ.ЗакрытьКанал(Канал);
КонецПроцедуры
```

#### ❌ Неправильно:

```bsl
// Не закрывать соединение = утечка ресурсов!
Подключение = КлиентRMQ.ПодключитьсяКСерверу(...);
// ... операции ...
// Забыли вызвать ОтключитьсяОтСервера()
```

---

### 2. Обработка ошибок

```bsl
Процедура ОтправитьСНадежностью(Данные)
    Попытка
        КлиентRMQ = Обработки.КлиентRMQ.Создать();
        Подключение = КлиентRMQ.ПодключитьсяКСерверу(СтрокаПодключения);
        
        Если КлиентRMQ.ЭтоОшибка(Подключение) Тогда
            ВызватьИсключение "Ошибка подключения: " + Подключение.Текст;
        КонецЕсли;
        
        Канал = КлиентRMQ.СоздатьКанал(Подключение, Истина); // Publisher Confirms
        КлиентRMQ.ОбъявитьОчередь(Канал, "queue", "", "", Истина); // durable
        
        Ответ = КлиентRMQ.ОпубликоватьСообщение(Канал, Данные, "queue", "", Истина, Истина); // persistent
        
        Если Ответ.Текст <> КлиентRMQ.ПодтверждениеСервера_СерверПодтвердилПолучениеСообщения() Тогда
            ВызватьИсключение "Сообщение не было подтверждено брокером";
        КонецЕсли;
        
        КлиентRMQ.ОтключитьсяОтСервера(Подключение);
    Исключение
        ЗаписьЛогаСистемы("RabbitMQ", ОписаниеОшибки(), , , УровеньЛогаСистемы.Ошибка);
        
        // Retry логика...
        ВызватьИсключение;
    КонецПопытки;
КонецПроцедуры
```

---

### 3. Логирование и отладка

```bsl
// Включить логирование
КлиентRMQ.ЗаписьЛога("C:\logs\rabbitmq_debug.log");

// Все операции компоненты будут логироваться

// Выключить логирование
КлиентRMQ.ЗаписьЛога("");
```

> **⚠️ Важно:** Логирование значительно замедляет работу! Используйте **только для отладки**.

---

### 4. Фоновая обработка

```bsl
// Регламентное задание "ОбработкаСообщений"
Процедура ОбработкаСообщений() Экспорт
    КлиентRMQ = Обработки.КлиентRMQ.Создать();
    Подключение = КлиентRMQ.ПодключитьсяКСерверу(Константы.RabbitMQ_ConnectionString.Получить());
    
    Попытка
        Канал = КлиентRMQ.СоздатьКанал(Подключение, Ложь, 1); // QoS = 1
        Получатель = КлиентRMQ.СоздатьПолучателя(Канал, "task_queue", "", Ложь, Ложь, Ложь, Ложь, 30000); // таймаут 30 сек
        
        Пока КлиентRMQ.СледующееСообщение(Получатель) Цикл
            ДанныеСообщения = КлиентRMQ.ДанныеСообщения();
            
            Если КлиентRMQ.ЭтоОшибка(ДанныеСообщения) Тогда
                Прервать; // ошибка или таймаут
            КонецЕсли;
            
            Попытка
                // Обработка
                ВыполнитьЗадачу(ДанныеСообщения.Данные);
                
                // Подтвердить
                КлиентRMQ.ПодтвердитьСообщение(Канал, ДанныеСообщения.Тег);
            Исключение
                // Отклонить и не возвращать (отправить в DLX)
                КлиентRMQ.ОтклонитьСообщение(Канал, ДанныеСообщения.Тег, Ложь);
                ЗаписьЛогаСистемы("RabbitMQ", ОписаниеОшибки());
            КонецПопытки;
        КонецЦикла;
    Исключение
        ЗаписьЛогаСистемы("RabbitMQ", ОписаниеОшибки());
    КонецПопытки;
    
    КлиентRMQ.ОтключитьсяОтСервера(Подключение);
КонецПроцедуры
```

---

### 5. Тестирование

```bsl
// Функция проверки связи
Функция ПроверитьСвязьСRabbitMQ() Экспорт
    КлиентRMQ = Обработки.КлиентRMQ.Создать();
    
    Попытка
        Подключение = КлиентRMQ.ПодключитьсяКСерверу("amqp://guest:guest@localhost:5672/%2f");
        
        Если КлиентRMQ.ЭтоОшибка(Подключение) Тогда
            Возврат Ложь;
        КонецЕсли;
        
        КлиентRMQ.ОтключитьсяОтСервера(Подключение);
        Возврат Истина;
    Исключение
        Возврат Ложь;
    КонецПопытки;
КонецФункции
```

---

## 🔧 Troubleshooting

### Компонента не подключается (Windows)

**Симптом:** Ошибка при вызове `ПодключитьКомпоненту()`

**Решения:**
1. Установите **Microsoft Visual C++ Redistributable**:  
   https://aka.ms/vs/17/release/vc_redist.x64.exe
2. Проверьте версию платформы: **8.3.21+** для Windows
3. Проверьте, что используется x64 версия 1С

### Ошибка "Не удается подключиться к серверу"

**Симптом:** Ошибка при вызове `ПодключитьсяКСерверу()`

**Решения:**
1. Проверьте, что RabbitMQ запущен:
   ```bash
   docker ps  # или
   netstat -an | grep 5672
   ```
2. Проверьте строку подключения:
   - Формат: `amqp://user:password@host:port/vhost`
   - Vhost по умолчанию: `%2f` (URL-encoded `/`)
3. Проверьте firewall и сетевые настройки

### Сообщения не доставляются

**Возможные причины:**

1. **Неверный routing key:**
   ```bsl
   // Для default exchange: routing key = имя очереди
   КлиентRMQ.ОпубликоватьСообщение(Канал, "Data", "queue_name");
   
   // Для custom exchange: проверьте binding
   ```

2. **Обменник не существует:**
   - Создайте через Management UI или используйте предустановленные (`amq.direct`, `amq.fanout`, `amq.topic`)

3. **Очередь не привязана к обменнику:**
   ```bsl
   // Проверьте параметры ОбъявитьОчередь
   КлиентRMQ.ОбъявитьОчередь(Канал, "queue", "routing_key", "exchange_name");
   ```

### Сообщения теряются при перезагрузке

**Решение:** Используйте durable + persistent:

```bsl
// Durable queue
КлиентRMQ.ОбъявитьОчередь(Канал, "queue", "", "", Истина);

// Persistent message
КлиентRMQ.ОпубликоватьСообщение(Канал, "Data", "queue", "", Истина, Истина);
```

### "Превышен лимит в 10'000 символов"

**Причина:** Используется бесплатная версия компоненты.

**Решения:**
1. Разбивайте сообщения на части
2. Используйте сжатие (`НастройкиСжатия`)
3. Приобретите полную версию

### QoS не работает

**Проверьте:**

1. QoS указан при создании канала:
   ```bsl
   Канал = КлиентRMQ.СоздатьКанал(Подключение, Ложь, 1);
   ```

2. Используется ручное подтверждение:
   ```bsl
   Получатель = КлиентRMQ.СоздатьПолучателя(Канал, "queue", "", Ложь); // no_ack = Ложь
   ```

3. Подтверждения отправляются:
   ```bsl
   КлиентRMQ.ПодтвердитьСообщение(Канал, ДанныеСообщения.Тег);
   ```

### "Компонента не работает из клиента"

**Симптом:** Ошибка при вызове методов компоненты из процедуры `&НаКлиенте`

**Причина:** Компонента работает **только на сервере**. Это ограничение платформы 1С.

**Решение:**

```bsl
// ❌ НЕПРАВИЛЬНО - не работает!
&НаКлиенте
Процедура Отправить(Команда)
    КлиентRMQ = Обработки.КлиентRMQ.Создать(); // Ошибка!
    // ...
КонецПроцедуры

// ✅ ПРАВИЛЬНО
&НаКлиенте
Процедура Отправить(Команда)
    ОтправитьНаСервере(); // Вызов серверной процедуры
КонецПроцедуры

&НаСервере
Процедура ОтправитьНаСервере()
    КлиентRMQ = Обработки.КлиентRMQ.Создать(); // Работает!
    // ...
КонецПроцедуры
```

Для фоновой обработки используйте:
- **Регламентные задания** (рекомендуется)
- **Фоновые задания** через `ФоновыеЗадания.Выполнить()`

**Причины:**

1. **Не закрываются соединения:**
   ```bsl
   // ВСЕГДА закрывайте!
   КлиентRMQ.ОтключитьсяОтСервера(Подключение);
   ```

2. **Блокирующий цикл в UI:**
   ```bsl
   // ❌ НЕ ДЕЛАЙТЕ ТАК!
   Пока КлиентRMQ.СледующееСообщение(Получатель) Цикл
       // ... в клиентском контексте
   КонецЦикла;
   
   // ✅ Используйте регламентное задание или фоновое задание
   ```

3. **Включено логирование в продакшене:**
   ```bsl
   // Отключите!
   КлиентRMQ.ЗаписьЛога("");
   ```

---

## 📞 Ссылки и контакты

### Документация
- 📚 **Руководства по RabbitMQ для 1С** (адаптация официальных туториалов):
  - ✅ [Урок 1: "Hello World!"](./lessons/Lesson1_ru.md) — основы отправки и получения сообщений
  - ✅ [Урок 2: "Work Queues"](./lessons/Lesson2_ru.md) — распределение задач, подтверждения, устойчивость, QoS
  - ✅ [Урок 3: "Publish/Subscribe"](./lessons/Lesson3_ru.md) — широковещательная рассылка через fanout exchange
  - ✅ [Урок 4: "Routing"](./lessons/Lesson4_ru.md) — точная маршрутизация через direct exchange
  - ✅ [Урок 5: "Topics"](./lessons/Lesson5_ru.md) — гибкая маршрутизация по шаблонам через topic exchange
  - ✅ [Урок 6: "RPC"](./lessons/Lesson6_ru.md) — синхронное взаимодействие с callback queue и correlation_id
  - ✅ [Урок 7: "Publisher Confirms"](./lessons/Lesson7_ru.md) — гарантия доставки сообщений
  - 🐳 [Docker setup](./lessons/DOCKER_README.md) — инструкции по запуску RabbitMQ через Docker
  
- 📖 [Полная документация API компоненты](./API_Documentation.md)
- 🌐 [Официальная документация RabbitMQ](https://www.rabbitmq.com/documentation.html)

### Релизы и загрузка
- 💾 [GitHub Releases](https://github.com/kovalevdmv/1CRabbitMQ/releases)

### Контакты автора
- 📱 [Telegram](https://t.me/kovalevdmv) — связь для приобретения полной версии
- 📢 [Telegram-канал: Внешние компоненты](https://t.me/tools1c)
- 🎥 [Telegram-канал: FastAbout1s](https://t.me/FastAbout1s) — видео и статьи
- 📝 [Обзорная статья на Dzen](https://dzen.ru/a/ZmSJHTYD0Gp9aQiS)
- 🎬 [Видео обзор](https://t.me/FastAbout1s/63)

### Сообщество
- 💬 [Обсуждение на Infostart](https://infostart.ru/)
- 🐛 [Issues на GitHub](https://github.com/kovalevdmv/1CRabbitMQ/issues)

---

## 📄 Лицензия

Компонента распространяется на коммерческой основе:
- **Бесплатная версия:** ограничение 10'000 символов на сообщение
- **Полная версия:** без ограничений (приобретается у автора)
- **Публичный договор оферты:** доступен в файле `offer.agreement.docx` в релизе

---
