# Руководство RabbitMQ - Урок 1: "Hello World!"

> **Источник:** [RabbitMQ Tutorial #1](https://www.rabbitmq.com/tutorials/tutorial-one-python)  
> **Адаптация для:** 1С:Предприятие  
> **Последнее обновление:** 25 декабря 2025 г.

---

## Введение (Introduction)

**RabbitMQ** — это брокер сообщений: он принимает и пересылает сообщения. Можно рассматривать его как почтовое отделение: когда вы опускаете письмо в почтовый ящик, вы уверены, что почтальон в конечном итоге доставит его получателю. В этой аналогии RabbitMQ выполняет роль почтового ящика, почтового отделения и почтальона одновременно.

Основное различие между RabbitMQ и реальной почтой заключается в том, что RabbitMQ не работает с бумагой — он принимает, хранит и пересылает **двоичные блоки данных** — сообщения.

### Терминология RabbitMQ

RabbitMQ и система обмена сообщениями в целом используют специфичную терминологию:

- **Производитель (Producer)** — программа, которая отправляет сообщения.

```
  [P] ----> (сообщение)
```

- **Очередь (Queue)** — "почтовый ящик" в RabbitMQ. Хотя сообщения передаются через RabbitMQ, они могут храниться только **внутри очереди**. Очередь ограничена только объемом памяти и диска хоста. По сути, это большой буфер сообщений.
  - Несколько производителей могут отправлять сообщения в одну очередь.
  - Несколько потребителей могут получать сообщения из одной очереди.

```
  ┌──────────────────┐
  │      hello       │  <- Очередь
  └──────────────────┘
```

- **Потребитель (Consumer)** — программа, которая ожидает и получает сообщения.

```
  (сообщение) ----> [C]
```

> **Важно:** Производитель, потребитель и брокер не обязательно должны находиться на одном хосте. В большинстве реальных приложений это не так. Одно приложение может быть одновременно и производителем, и потребителем.

---

## Установка и подготовка (Setup)

### 1. Установка RabbitMQ

Для работы с примерами вам понадобится запущенный сервер RabbitMQ.

#### Вариант А: Установка через Docker (рекомендуется)

> **📘 Подробная инструкция:** См. [DOCKER_README.md](./DOCKER_README.md) — полное руководство по настройке Docker

После запуска RabbitMQ Management UI будет доступен по адресу: `http://localhost:15672`  
(Логин: `rmuser`, Пароль: `rmpassword`)

#### Вариант Б: Нативная установка

Следуйте [официальной инструкции по установке RabbitMQ](https://www.rabbitmq.com/download.html) для вашей операционной системы.

### 2. Подключение компоненты 1С

В этом руководстве используется внешняя компонента [kovalevdmv/1CRabbitMQ](https://github.com/kovalevdmv/1CRabbitMQ).

**Шаги подключения:**

1. Скачайте компоненту из репозитория
2. Разместите файлы компоненты в каталоге, доступном для платформы 1С
3. В вашей конфигурации используйте обработку `КлиентRMQ` для работы с RabbitMQ

> **Требования:**  
> - Платформа 1С:Предприятие 8.3.21 или выше (для Windows)
> - Компонента работает **только в серверном контексте** (`&НаСервере`, регламентные задания, фоновые задания)

### 3. Библиотека для работы с RabbitMQ

В оригинальном руководстве на Python используется библиотека **Pika**. В 1С мы используем компоненту через объектную обработку `КлиентRMQ`, которая предоставляет удобный API для работы с RabbitMQ.

---

## "Привет, мир!" (Hello World!)

В этой части урока мы напишем две небольшие программы на языке 1С:
- **Производитель (sender)** — отправляет одно сообщение
- **Потребитель (receiver)** — получает сообщения и выводит их

Это "Hello World" системы обмена сообщениями.

### Общая схема работы

```
  [P] ----> [hello] ----> [C]
  
  P = Производитель (Producer)
  C = Потребитель (Consumer)
  [hello] = Очередь (Queue)
```

Производитель отправляет сообщения в очередь `hello`. Потребитель получает сообщения из этой очереди.

---

## Отправка сообщений (Sending)

```
  [P] ----> [hello]
```

Наша первая программа отправит одно сообщение в очередь.

### Шаг 1: Подключение к RabbitMQ

Первое, что нам нужно сделать — установить соединение с сервером RabbitMQ.

```bsl
// Создаем экземпляр клиента RabbitMQ
КлиентRMQ = Обработки.КлиентRMQ.Создать();

// Формируем строку подключения
СтрокаПодключения = "amqp://rmuser:rmpassword@localhost:5672/%2F";

// Устанавливаем соединение с сервером
Подключение = КлиентRMQ.ПодключитьсяКСерверу(СтрокаПодключения);

// Обрабатываем возможные ошибки подключения
Если КлиентRMQ.ЭтоОшибка(Подключение) Тогда
    КлиентRMQ.ОтключитьсяОтСервера(Подключение);
    ВызватьИсключение "Ошибка подключения к RabbitMQ";
КонецЕсли;

// Создаем канал для обмена сообщениями
Канал = КлиентRMQ.СоздатьКанал(Подключение);
```

**Пояснения:**

- `КлиентRMQ` — объектная обработка, предоставляющая API для работы с RabbitMQ
- `СтрокаПодключения` — URI в формате AMQP: `amqp://пользователь:пароль@хост:порт/виртуальныйХост`
  - `%2F` — это URL-кодированное значение для виртуального хоста `/` (по умолчанию)
- `Подключение` — объект соединения с брокером
- `Канал` — логический канал для выполнения операций (объявление очередей, отправка сообщений)

> **Виртуальные хосты (Virtual Hosts):**  
> RabbitMQ — мультитенантная система. Соединения, обменники, очереди, привязки и разрешения принадлежат виртуальным хостам — логическим группам объектов. Если вы знакомы с виртуальными хостами в Apache или server blocks в Nginx, идея аналогична. Подробнее в [документации RabbitMQ](https://www.rabbitmq.com/vhosts.html).

### Шаг 2: Объявление очереди

Перед отправкой сообщения нужно убедиться, что очередь получателя существует. Если отправить сообщение в несуществующую очередь, RabbitMQ просто удалит это сообщение.

```bsl
// Параметры очереди
ИмяОчереди = "hello";
КлючМаршрутизации = "";  // Пока не используется
ТочкаОбмена = "";         // Пустая строка = обменник по умолчанию (AMQP default)
СохранятьОчередь = Ложь;  // False = очередь не переживет перезапуск RabbitMQ

// Объявляем очередь
Очередь = КлиентRMQ.ОбъявитьОчередь(Канал, ИмяОчереди, КлючМаршрутизации, 
    ТочкаОбмена, СохранятьОчередь);
```

**Пояснения:**

- `ОбъявитьОчередь()` — создает очередь, если она не существует
- **Идемпотентность:** Повторные вызовы с одинаковыми параметрами безопасны — очередь создается только один раз
- `СохранятьОчередь = Ложь` — очередь не является **durable** (устойчивой), она будет удалена при перезапуске RabbitMQ
  - Тему устойчивости мы подробно рассмотрим в Уроке 2

> **Важно:** Если очередь уже существует с другими параметрами (например, `СохранятьОчередь = Истина`), сервер закроет канал с ошибкой. Параметры существующей очереди должны совпадать с параметрами в `ОбъявитьОчередь()`.

### Шаг 3: Отправка сообщения

Теперь мы готовы отправить сообщение.

```bsl
// Данные сообщения
Данные = "Hello World!";

// Параметры публикации
КлючМаршрутизации = "hello";  // Имя очереди
ТочкаОбмена = "";              // Обменник по умолчанию

// Отправляем сообщение
Ответ = КлиентRMQ.ОпубликоватьСообщение(Канал, Данные, КлючМаршрутизации, ТочкаОбмена);

// Проверяем результат отправки
Если КлиентRMQ.ЭтоОшибка(Ответ) Тогда
    ВызватьИсключение СтрШаблон("Ошибка при отправке сообщения: %1", Ответ.Текст);
КонецЕсли;

Сообщить(" [x] Отправлено 'Hello World!'");
```

**Пояснения:**

- В RabbitMQ сообщение **никогда не отправляется напрямую в очередь** — оно всегда проходит через **обменник (exchange)**
- Пустая строка `ТочкаОбмена = ""` означает использование **обменника по умолчанию** (default exchange)
- Обменник по умолчанию — особенный: он маршрутизирует сообщение в очередь, имя которой совпадает с `КлючМаршрутизации`
- Подробнее об обменниках мы поговорим в Уроке 3

> **Сравнение с Python (Pika):**
> 
> Python:
> ```python
> channel.basic_publish(exchange='', routing_key='hello', body='Hello World!')
> ```
> 
> 1С:
> ```bsl
> КлиентRMQ.ОпубликоватьСообщение(Канал, Данные, КлючМаршрутизации, ТочкаОбмена)
> ```

### Шаг 4: Закрытие соединения

Перед завершением программы необходимо закрыть соединение, чтобы:
- Очистить сетевые буферы
- Убедиться, что сообщение действительно доставлено в RabbitMQ
- Освободить ресурсы на сервере

```bsl
// !!! ОБЯЗАТЕЛЬНО закрываем соединение
КлиентRMQ.ОтключитьсяОтСервера(Подключение);
```

> **⚠️ Важно:** Незакрытые соединения приводят к утечке памяти и ресурсов на стороне RabbitMQ.

### Полный код отправителя

```bsl
// Процедура отправки сообщения (должна быть &НаСервере)
&НаСервере
Процедура ОтправитьСообщение()
    
    // 1. Создаем клиент и подключаемся
    КлиентRMQ = Обработки.КлиентRMQ.Создать();
    СтрокаПодключения = "amqp://rmuser:rmpassword@localhost:5672/%2F";
    Подключение = КлиентRMQ.ПодключитьсяКСерверу(СтрокаПодключения);
    
    Если КлиентRMQ.ЭтоОшибка(Подключение) Тогда
        КлиентRMQ.ОтключитьсяОтСервера(Подключение);
        ВызватьИсключение "Ошибка подключения к RabbitMQ";
    КонецЕсли;
    
    // 2. Создаем канал
    Канал = КлиентRMQ.СоздатьКанал(Подключение);
    
    // 3. Объявляем очередь
    ИмяОчереди = "hello";
    Очередь = КлиентRMQ.ОбъявитьОчередь(Канал, ИмяОчереди, "", "", Ложь);
    
    // 4. Отправляем сообщение
    Данные = "Hello World!";
    Ответ = КлиентRMQ.ОпубликоватьСообщение(Канал, Данные, ИмяОчереди, "");
    
    Если КлиентRMQ.ЭтоОшибка(Ответ) Тогда
        КлиентRMQ.ОтключитьсяОтСервера(Подключение);
        ВызватьИсключение СтрШаблон("Ошибка отправки: %1", Ответ.Текст);
    КонецЕсли;
    
    Сообщить(" [x] Отправлено 'Hello World!'");
    
    // 5. Закрываем соединение
    КлиентRMQ.ОтключитьсяОтСервера(Подключение);
    
КонецПроцедуры
```

---

## Получение сообщений (Receiving)

```
  [hello] ----> [C]
```

Наша вторая программа будет получать сообщения из очереди и выводить их на экран.

### Шаг 1: Подключение к RabbitMQ

Код подключения идентичен коду отправителя:

```bsl
КлиентRMQ = Обработки.КлиентRMQ.Создать();
СтрокаПодключения = "amqp://rmuser:rmpassword@localhost:5672/%2F";
Подключение = КлиентRMQ.ПодключитьсяКСерверу(СтрокаПодключения);

Если КлиентRMQ.ЭтоОшибка(Подключение) Тогда
    КлиентRMQ.ОтключитьсяОтСервера(Подключение);
    ВызватьИсключение "Ошибка подключения к RabbitMQ";
КонецЕсли;

Канал = КлиентRMQ.СоздатьКанал(Подключение);
```

### Шаг 2: Объявление очереди

Как и в отправителе, нужно убедиться, что очередь существует:

```bsl
ИмяОчереди = "hello";
Очередь = КлиентRMQ.ОбъявитьОчередь(Канал, ИмяОчереди, "", "", Ложь);
```

**Почему мы объявляем очередь снова?**

Мы могли бы избежать этого, если бы были уверены, что очередь уже существует (например, если отправитель был запущен ранее). Но мы не знаем, какая программа будет запущена первой. **Хорошая практика** — объявлять очередь в обеих программах.

> **Идемпотентность:** Вызов `ОбъявитьОчередь()` с одинаковыми параметрами безопасен. Если очередь уже существует с такими же параметрами — ничего не произойдет. Если параметры отличаются — сервер закроет канал с ошибкой.

### Шаг 3: Создание получателя

В Python библиотека Pika использует **callback-функцию** (функцию обратного вызова), которая вызывается асинхронно при получении каждого сообщения:

```python
def callback(ch, method, properties, body):
    print(f" [x] Received {body}")

channel.basic_consume(queue='hello', on_message_callback=callback, auto_ack=True)
channel.start_consuming()  # Бесконечный цикл
```

В 1С компонента `КлиентRMQ` использует **блокирующий (синхронный) подход**:

```bsl
// Создаем получателя
ТегПолучателя = "";       // Пустая строка — сервер сгенерирует уникальный тег
no_ack = Истина;          // Истина = автоматическое подтверждение получения

Получатель = КлиентRMQ.СоздатьПолучателя(Канал, ИмяОчереди, ТегПолучателя, no_ack);
```

**Пояснения:**

- `СоздатьПолучателя()` — регистрирует потребителя для очереди
- `ТегПолучателя` — уникальный идентификатор потребителя (пустая строка = генерируется автоматически)
- `no_ack = Истина` — режим **автоматического подтверждения** (auto-acknowledgment)
  - RabbitMQ считает сообщение доставленным сразу после отправки потребителю
  - Подробнее о подтверждениях — в Уроке 2

### Шаг 4: Получение сообщений в цикле

Функция `СледующееСообщение()` **блокирует выполнение потока**, ожидая появления сообщения в очереди:

```bsl
// Цикл ожидания и обработки сообщений
Пока КлиентRMQ.СледующееСообщение(Получатель) Цикл
    
    // Получаем данные сообщения
    ДанныеСообщения = КлиентRMQ.ДанныеСообщения();
    
    // Проверяем на ошибки
    Если КлиентRMQ.ЭтоОшибка(ДанныеСообщения) Тогда
        Продолжить;
    КонецЕсли;
    
    // Выводим содержимое сообщения
    Сообщить(" [x] Получено: " + ДанныеСообщения.Данные);
    
    // Для примера обрабатываем только одно сообщение
    Прервать;
    
КонецЦикла;
```

**Пояснения:**

- `СледующееСообщение(Получатель)` — блокирует поток и ждет сообщения
  - Возвращает `Истина`, если сообщение получено
  - Возвращает `Ложь`, если произошла ошибка
- `ДанныеСообщения()` — получает структуру с данными последнего сообщения
- **⚠️ Важно:** Этот цикл блокирует поток! Его нужно запускать в **фоновом задании** или **регламентном задании**

### Шаг 5: Обработка ошибок и закрытие

```bsl
// Проверка на ошибки после выхода из цикла
ДанныеСообщения = КлиентRMQ.ДанныеСообщения();
Если КлиентRMQ.ЭтоОшибка(ДанныеСообщения) Тогда
    // Обработка ошибки (например, потеря соединения)
    Сообщить("Ошибка при получении сообщения: " + ДанныеСообщения.Текст);
КонецЕсли;

// Закрываем соединение
КлиентRMQ.ОтключитьсяОтСервера(Подключение);
```

### Полный код получателя

```bsl
// Процедура получения сообщения (должна быть &НаСервере или в фоновом задании)
&НаСервере
Процедура ПолучитьСообщение()
    
    // 1. Создаем клиент и подключаемся
    КлиентRMQ = Обработки.КлиентRMQ.Создать();
    СтрокаПодключения = "amqp://rmuser:rmpassword@localhost:5672/%2F";
    Подключение = КлиентRMQ.ПодключитьсяКСерверу(СтрокаПодключения);
    
    Если КлиентRMQ.ЭтоОшибка(Подключение) Тогда
        КлиентRMQ.ОтключитьсяОтСервера(Подключение);
        ВызватьИсключение "Ошибка подключения к RabbitMQ";
    КонецЕсли;
    
    // 2. Создаем канал
    Канал = КлиентRMQ.СоздатьКанал(Подключение);
    
    // 3. Объявляем очередь
    ИмяОчереди = "hello";
    Очередь = КлиентRMQ.ОбъявитьОчередь(Канал, ИмяОчереди, "", "", Ложь);
    
    // 4. Создаем получателя
    Получатель = КлиентRMQ.СоздатьПолучателя(Канал, ИмяОчереди, "", Истина);
    
    // 5. Получаем сообщения в цикле
    Сообщить(" [*] Ожидание сообщений...");
    
    Пока КлиентRMQ.СледующееСообщение(Получатель) Цикл
        
        ДанныеСообщения = КлиентRMQ.ДанныеСообщения();
        
        Если КлиентRMQ.ЭтоОшибка(ДанныеСообщения) Тогда
            Продолжить;
        КонецЕсли;
        
        Сообщить(" [x] Получено: " + ДанныеСообщения.Данные);
        
        // Для примера обрабатываем только одно сообщение
        Прервать;
        
    КонецЦикла;
    
    // 6. Обработка ошибок после цикла
    ДанныеСообщения = КлиентRMQ.ДанныеСообщения();
    Если КлиентRMQ.ЭтоОшибка(ДанныеСообщения) Тогда
        Сообщить("Ошибка: " + ДанныеСообщения.Текст);
    КонецЕсли;
    
    // 7. Закрываем соединение
    КлиентRMQ.ОтключитьсяОтСервера(Подключение);
    
КонецПроцедуры
```

---

## Ключевые отличия от Python (Pika)

| Аспект | Python (Pika) | 1С (КлиентRMQ) |
|--------|---------------|----------------|
| **Модель получения** | Асинхронная (callback) | Синхронная (блокирующий цикл) |
| **Код получения** | `channel.start_consuming()` | `Пока СледующееСообщение() Цикл` |
| **Контекст выполнения** | Клиент/Сервер | **Только сервер** |
| **Многопоточность** | Встроенная в Pika | Требуется фоновое задание |

**Важное следствие для 1С:**

Поскольку `СледующееСообщение()` блокирует поток, получатель должен запускаться в:
- **Фоновом задании** — для однократного или периодического запуска
- **Регламентном задании** — для постоянного мониторинга очереди

---

## Собираем всё вместе (Putting It All Together)

### Тестирование примера

#### Шаг 1: Запустите получателя

Создайте фоновое задание, которое запускает процедуру `ПолучитьСообщение()`:

```bsl
&НаКлиенте
Процедура ЗапуститьПолучателя(Команда)
    ЗапуститьПолучателяНаСервере();
КонецПроцедуры

&НаСервере
Процедура ЗапуститьПолучателяНаСервере()
    ФоновыеЗадания.Выполнить("ПолучитьСообщение");
КонецПроцедуры
```

Вы увидите сообщение:
```
[*] Ожидание сообщений...
```

#### Шаг 2: Отправьте сообщение

В другой форме (или в том же сеансе) вызовите процедуру `ОтправитьСообщение()`:

```bsl
&НаКлиенте
Процедура ОтправитьСообщение(Команда)
    ОтправитьСообщениеНаСервере();
КонецПроцедуры

&НаСервере
Процедура ОтправитьСообщениеНаСервере()
    ОтправитьСообщение();
КонецПроцедуры
```

Вы увидите:
```
[x] Отправлено 'Hello World!'
```

#### Шаг 3: Проверьте результат

Получатель выведет:
```
[*] Ожидание сообщений...
[x] Получено: Hello World!
```

### Проверка очередей через Web UI

Откройте RabbitMQ Management UI: `http://localhost:15672`

Перейдите на вкладку **Queues** — вы увидите очередь `hello` и статистику сообщений.

### Проверка очередей через командную строку

**Linux/macOS:**
```bash
sudo rabbitmqctl list_queues
```

**Windows:**
```powershell
rabbitmqctl.bat list_queues
```

Вывод:
```
Listing queues for vhost / ...
name    messages
hello   0
```

---

## Устранение проблем

### Сообщение не отправляется

Если вы не видите подтверждения отправки, возможные причины:

1. **Недостаточно места на диске**  
   RabbitMQ требует минимум 50 МБ свободного места. Проверьте логи:
   ```bash
   docker logs rabbitmq
   ```

2. **Неверные учетные данные**  
   Проверьте логин/пароль в строке подключения

3. **RabbitMQ не запущен**  
   Проверьте статус контейнера:
   ```bash
   docker ps
   ```

4. **Платформа 1С старше 8.3.21**  
   Компонента требует версию не ниже 8.3.21 (для Windows)

---

## Что дальше?

Поздравляем! Вы научились:
- ✅ Подключаться к RabbitMQ из 1С
- ✅ Создавать очереди
- ✅ Отправлять сообщения
- ✅ Получать сообщения

### Следующий шаг: Урок 2 — "Work Queues" (Рабочие очереди)

В следующем уроке мы:
- Создадим **рабочую очередь** для распределения задач между несколькими воркерами
- Научимся **подтверждать получение сообщений** (message acknowledgment)
- Сделаем сообщения и очереди **устойчивыми** к перезапуску RabbitMQ (durability)
- Реализуем **справедливое распределение** задач (fair dispatch с QoS)

Переходите к [Уроку 2](./Lesson2_ru.md)!

---

## Дополнительные материалы

- [Официальная документация RabbitMQ](https://www.rabbitmq.com/documentation.html)
- [Репозиторий компоненты kovalevdmv/1CRabbitMQ](https://github.com/kovalevdmv/1CRabbitMQ)
- [Полная документация API компоненты](./Component_API_Documentation.md)
- [Концепция виртуальных хостов в RabbitMQ](https://www.rabbitmq.com/vhosts.html)

---

**Примечание:** Полный рабочий пример в виде обработки `Урок1_HelloWorld.epf` будет добавлен после завершения всех уроков.
