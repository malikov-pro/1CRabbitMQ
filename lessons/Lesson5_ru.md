# Руководство RabbitMQ - Урок 5: "Topics" (Темы)

## Введение (Introduction)

### Предварительные требования

Убедитесь, что:
- ✅ RabbitMQ запущен (см. [DOCKER_README.md](./DOCKER_README.md))
- ✅ Компонента `КлиентRMQ` подключена к конфигурации
- ✅ Вы изучили [Урок 1](./Lesson1_ru.md), [Урок 2](./Lesson2_ru.md), [Урок 3](./Lesson3_ru.md) и [Урок 4](./Lesson4_ru.md)

### Чему посвящен этот урок

В [предыдущем уроке](./Lesson4_ru.md) мы улучшили нашу систему логирования. Вместо обменника `fanout`, способного только на бездумную трансляцию, мы использовали `direct`, что позволило нам избирательно получать логи.

Хотя использование обменника `direct` улучшило нашу систему, у неё всё ещё есть **ограничения** — она не может выполнять маршрутизацию **по нескольким критериям**.

В нашей системе логирования мы можем захотеть подписаться не только на логи по уровню важности, но и **по источнику**, который создал лог. Эта концепция известна из Unix-утилиты [`syslog`](https://en.wikipedia.org/wiki/Syslog), которая маршрутизирует логи на основе **серьёзности** (info/warn/crit...) и **подсистемы** (auth/cron/kern...).

Это даст нам большую гибкость — мы можем слушать только критические ошибки из 'cron', но при этом все логи из 'kern'.

Чтобы реализовать это в нашей системе логирования, нам нужно изучить более сложный обменник типа **`topic`**.

---

## Обменник Topic

Сообщения, отправляемые в обменник `topic`, не могут иметь произвольный `routing_key` — это должен быть **список слов, разделённых точками**. Слова могут быть любыми, но обычно они описывают какие-то характеристики сообщения.

### Примеры валидных ключей маршрутизации:

- `"склад.поступления.срочно"`
- `"документы.заказ.создан"`
- `"уведомление.email.высокий"`
- `"stock.usd.nyse"`
- `"quick.orange.rabbit"`

Может быть **столько слов**, сколько вам нужно, до предела в **255 байт**.

### Специальные символы в ключах привязки

Ключ привязки (binding key) также должен быть в том же формате. Логика обменника `topic` похожа на `direct` — сообщение с определённым `routing_key` будет доставлено во все очереди, привязанные с совпадающим `binding key`. Но есть **два важных специальных случая**:

- **`*` (звёздочка)** — заменяет **ровно одно слово**
- **`#` (решётка)** — заменяет **ноль или более слов**

---

## Пример работы Topic Exchange

```
                     [Exchange: topic_logs (type: topic)]
                                    |
         +--------------------------+-------------------------+
         |                          |                         |
    (*.orange.*)                (*.*.rabbit)              (lazy.#)
         |                          |                         |
         v                          v                         v
      [Q1]                       [Q2]                       [Q2]
```

В этом примере мы отправляем сообщения о животных. Ключ маршрутизации состоит из трёх слов (две точки): `<скорость>.<цвет>.<вид>`.

### Привязки:

- **Q1** привязана с ключом **`*.orange.*`** — интересуют все **оранжевые животные**
- **Q2** привязана с ключами **`*.*.rabbit`** и **`lazy.#`** — интересуют все **кролики** и все **ленивые животные**

### Примеры маршрутизации:

| Routing Key | Куда попадёт | Почему |
|------------|-------------|--------|
| `quick.orange.rabbit` | **Q1 и Q2** | Совпадает с `*.orange.*` и `*.*.rabbit` |
| `lazy.orange.elephant` | **Q1 и Q2** | Совпадает с `*.orange.*` и `lazy.#` |
| `quick.orange.fox` | **Q1** | Совпадает только с `*.orange.*` |
| `lazy.brown.fox` | **Q2** | Совпадает только с `lazy.#` |
| `lazy.pink.rabbit` | **Q2 один раз** | Совпадает с обоими шаблонами Q2, но доставка однократная |
| `quick.brown.fox` | **Отброшено** | Не совпадает ни с одним шаблоном |

### Граничные случаи:

**Одно слово (`orange`) или четыре слова (`quick.orange.new.rabbit`):**
- `orange` — не совпадёт ни с одним шаблоном → отброшено
- `quick.orange.new.rabbit` — не совпадёт с `*.orange.*` и `*.*.rabbit`, но **совпадёт с `lazy.#`?** Нет, т.к. не начинается с `lazy` → отброшено

**Четыре слова, начинающиеся с `lazy`:**
- `lazy.orange.new.rabbit` — совпадёт с `lazy.#` → доставлено в Q2

---

## Универсальность Topic Exchange

> **💡 Важно:** Обменник `topic` — это мощный инструмент, который может вести себя как другие обменники:
>
> - Когда очередь привязана с ключом **`#`** — она получит **все сообщения** (как `fanout`)
> - Когда в привязках **не используются `*` и `#`** — обменник работает как `direct`

---

## Создание обменника Topic в 1С

Для работы нам понадобится обменник типа `topic`.

### Вариант А: Создание через Management UI (рекомендуется)

1. Откройте RabbitMQ Management UI: http://localhost:15672
2. Войдите (логин: `rmuser`, пароль: `rmpassword`)
3. Перейдите на вкладку **"Exchanges"**
4. Нажмите **"Add a new exchange"**
5. Заполните поля:
   - **Name:** `topic_logs`
   - **Type:** `topic`
   - **Durability:** `Durable` (рекомендуется)
   - **Auto delete:** `No`
6. Нажмите **"Add exchange"**

### Вариант Б: Использование предустановленного обменника

Компонента предоставляет доступ к предустановленному обменнику:

```bsl
ТочкаОбмена = КлиентRMQ.Exchange_topic(); // Вернет "amq.topic"
```

> **💡 Рекомендация:** Для production используйте **Вариант А** с семантическим именем `topic_logs`. Для тестирования подойдет **Вариант Б**.

---

## Собираем всё вместе

Мы будем использовать обменник `topic` для нашей системы логирования. Предположим, что ключи маршрутизации логов имеют два слова: **`<подсистема>.<важность>`**.

### Примеры ключей для 1С:

- `"склад.error"` — ошибка в подсистеме "Склад"
- `"документы.warning"` — предупреждение в подсистеме "Документы"
- `"обмен.info"` — информация об обмене данными
- `"регламент.error"` — ошибка регламентного задания

---

## Код издателя (emit_log_topic)

```bsl
&НаСервере
Процедура ОтправитьЛогПоТемеНаСервере(Подсистема, УровеньВажности, ТекстСообщения)
    
    // 1. Формируем routing_key в формате "<подсистема>.<важность>"
    КлючМаршрутизации = СтрШаблон("%1.%2", Подсистема, УровеньВажности);
    
    // 2. Подключение
    КлиентRMQ = Обработки.КлиентRMQ.Создать();
    Подключение = КлиентRMQ.ПодключитьсяКСерверу("amqp://rmuser:rmpassword@localhost:5672/%2f");
    
    Если КлиентRMQ.ЭтоОшибка(Подключение) Тогда
        ВызватьИсключение "Ошибка подключения к RabbitMQ";
    КонецЕсли;
    
    // 3. Создание канала
    Канал = КлиентRMQ.СоздатьКанал(Подключение);
    
    // 4. Публикация с routing_key
    //    Вариант А: Пользовательский обменник
    ТочкаОбмена = "topic_logs";
    
    //    Вариант Б: Предустановленный обменник
    //    ТочкаОбмена = КлиентRMQ.Exchange_topic();
    
    Ответ = КлиентRMQ.ОпубликоватьСообщение(
        Канал, 
        ТекстСообщения,      // Данные
        КлючМаршрутизации,   // routing_key: "склад.error", "документы.warning" и т.д.
        ТочкаОбмена          // Обменник типа topic
    );
    
    Если КлиентRMQ.ЭтоОшибка(Ответ) Тогда
        КлиентRMQ.ОтключитьсяОтСервера(Подключение);
        ВызватьИсключение "Ошибка публикации: " + Ответ.Текст;
    КонецЕсли;
    
    Сообщить(СтрШаблон(" [x] Отправлено [%1]: %2", КлючМаршрутизации, ТекстСообщения));
    
    // 5. Отключение
    КлиентRMQ.ОтключитьсяОтСервера(Подключение);
    
КонецПроцедуры

// Клиентские команды для отправки разных логов
&НаКлиенте
Процедура ОтправитьЛогСклада(Команда)
    ОтправитьЛогПоТемеНаСервере("склад", "error", "Критическая ошибка: товар не найден!");
КонецПроцедуры

&НаКлиенте
Процедура ОтправитьЛогДокументов(Команда)
    ОтправитьЛогПоТемеНаСервере("документы", "warning", "Предупреждение: документ без номера");
КонецПроцедуры

&НаКлиенте
Процедура ОтправитьЛогОбмена(Команда)
    ОтправитьЛогПоТемеНаСервере("обмен", "info", "Обмен завершён успешно");
КонецПроцедуры

&НаКлиенте
Процедура ОтправитьЛогРегламента(Команда)
    ОтправитьЛогПоТемеНаСервере("регламент", "error", "Регламентное задание завершилось с ошибкой");
КонецПроцедуры
```

---

## Код подписчика (receive_logs_topic)

```bsl
// ⚠️ Этот код должен выполняться в РЕГЛАМЕНТНОМ ЗАДАНИИ или ФОНОВОМ ЗАДАНИИ!

&НаСервере
Процедура ПолучатьЛогиПоШаблонамВФоне(МассивШаблонов) Экспорт
    
    // МассивШаблонов: ["склад.#"], ["*.error"], ["склад.*", "документы.*"] и т.д.
    
    Если МассивШаблонов.Количество() = 0 Тогда
        ВызватьИсключение "Необходимо указать хотя бы один шаблон для подписки";
    КонецЕсли;
    
    // 1. Подключение
    КлиентRMQ = Обработки.КлиентRMQ.Создать();
    Подключение = КлиентRMQ.ПодключитьсяКСерверу("amqp://rmuser:rmpassword@localhost:5672/%2f");
    
    Если КлиентRMQ.ЭтоОшибка(Подключение) Тогда
        ВызватьИсключение "Ошибка подключения к RabbitMQ";
    КонецЕсли;
    
    Попытка
        // 2. Создание канала
        Канал = КлиентRMQ.СоздатьКанал(Подключение);
        
        //    Вариант А: Пользовательский обменник
        ТочкаОбмена = "topic_logs";
        
        //    Вариант Б: Предустановленный обменник
        //    ТочкаОбмена = КлиентRMQ.Exchange_topic();
        
        // 3. Создание временной очереди и привязок с шаблонами
        ИмяОчереди = "";
        
        Для Индекс = 0 По МассивШаблонов.ВГраница() Цикл
            ШаблонПривязки = МассивШаблонов[Индекс];
            
            Если Индекс = 0 Тогда
                // Первый вызов - создает очередь и первую привязку
                Очередь = КлиентRMQ.ОбъявитьОчередь(
                    Канал,
                    "",                  // Пустое имя - сервер сгенерирует
                    ШаблонПривязки,      // Ключ привязки (binding key) с шаблонами *, #
                    ТочкаОбмена,
                    Ложь,                // durable
                    Истина,              // exclusive
                    Истина               // auto-delete
                );
                
                ИмяОчереди = Очередь.ИмяОчереди;
                Сообщить(СтрШаблон(" [*] Создана очередь: %1", ИмяОчереди));
                Сообщить(СтрШаблон(" [*] Подписка на шаблон: '%1'", ШаблонПривязки));
            Иначе
                // Последующие вызовы - добавляют привязки к существующей очереди
                КлиентRMQ.ОбъявитьОчередь(
                    Канал,
                    ИмяОчереди,          // Имя существующей очереди
                    ШаблонПривязки,      // Новый ключ привязки с шаблонами
                    ТочкаОбмена,
                    Ложь,
                    Истина,
                    Истина
                );
                
                Сообщить(СтрШаблон(" [*] Добавлена подписка на шаблон: '%1'", ШаблонПривязки));
            КонецЕсли;
        КонецЦикла;
        
        // 4. Создание получателя
        Получатель = КлиентRMQ.СоздатьПолучателя(
            Канал, 
            ИмяОчереди,
            "",      // ТэгПолучателя (автоматический)
            Истина   // no_ack = Истина (автоподтверждение)
        );
        
        Сообщить(" [*] Ожидание логов. Для выхода нажмите Ctrl+C");
        
        // 5. Цикл получения сообщений
        Пока КлиентRMQ.СледующееСообщение(Получатель) Цикл
            
            ДанныеСообщения = КлиентRMQ.ДанныеСообщения();
            
            Если КлиентRMQ.ЭтоОшибка(ДанныеСообщения) Тогда
                Прервать;
            КонецЕсли;
            
            // Обработка полученного лога
            // В компоненте routing_key доступен через свойства сообщения (если они были запрошены)
            Сообщить(СтрШаблон(" [x] Получено: %1", ДанныеСообщения.Данные));
            
        КонецЦикла;
        
    Исключение
        ЗаписьЛогаСистемы("RabbitMQ", ОписаниеОшибки(), , , УровеньЛогаСистемы.Ошибка);
    КонецПопытки;
    
    // 6. Отключение
    КлиентRMQ.ОтключитьсяОтСервера(Подключение);
    
КонецПроцедуры
```

---

## Запуск и тестирование

### Шаг 1: Создайте обменник

Через Management UI создайте обменник:
- **Name:** `topic_logs`
- **Type:** `topic`

### Шаг 2: Подписчик #1 — Все логи

```bsl
&НаКлиенте
Процедура ЗапуститьПодписчикаНаВсеЛоги(Команда)
    ШаблоныДляПодписки = Новый Массив;
    ШаблоныДляПодписки.Добавить("#"); // Все сообщения
    
    ПараметрыЗадания = Новый Массив;
    ПараметрыЗадания.Добавить(ШаблоныДляПодписки);
    
    ФоновыеЗадания.Выполнить("ПолучатьЛогиПоШаблонамВФоне", ПараметрыЗадания, , "Подписчик: все логи");
    Сообщить("Запущен подписчик на шаблон: #");
КонецПроцедуры
```

### Шаг 3: Подписчик #2 — Все логи подсистемы "склад"

```bsl
&НаКлиенте
Процедура ЗапуститьПодписчикаНаСклад(Команда)
    ШаблоныДляПодписки = Новый Массив;
    ШаблоныДляПодписки.Добавить("склад.#"); // Все логи, начинающиеся с "склад."
    
    ПараметрыЗадания = Новый Массив;
    ПараметрыЗадания.Добавить(ШаблоныДляПодписки);
    
    ФоновыеЗадания.Выполнить("ПолучатьЛогиПоШаблонамВФоне", ПараметрыЗадания, , "Подписчик: склад");
    Сообщить("Запущен подписчик на шаблон: склад.#");
КонецПроцедуры
```

### Шаг 4: Подписчик #3 — Только критические ошибки из любой подсистемы

```bsl
&НаКлиенте
Процедура ЗапуститьПодписчикаНаОшибки(Команда)
    ШаблоныДляПодписки = Новый Массив;
    ШаблоныДляПодписки.Добавить("*.error"); // Любая подсистема, уровень "error"
    
    ПараметрыЗадания = Новый Массив;
    ПараметрыЗадания.Добавить(ШаблоныДляПодписки);
    
    ФоновыеЗадания.Выполнить("ПолучатьЛогиПоШаблонамВФоне", ПараметрыЗадания, , "Подписчик: ошибки");
    Сообщить("Запущен подписчик на шаблон: *.error");
КонецПроцедуры
```

### Шаг 5: Подписчик #4 — Несколько шаблонов

```bsl
&НаКлиенте
Процедура ЗапуститьПодписчикаНаСкладИДокументы(Команда)
    ШаблоныДляПодписки = Новый Массив;
    ШаблоныДляПодписки.Добавить("склад.*");      // Все логи склада
    ШаблоныДляПодписки.Добавить("документы.*");  // Все логи документов
    
    ПараметрыЗадания = Новый Массив;
    ПараметрыЗадания.Добавить(ШаблоныДляПодписки);
    
    ФоновыеЗадания.Выполнить("ПолучатьЛогиПоШаблонамВФоне", ПараметрыЗадания, , "Подписчик: склад+документы");
    Сообщить("Запущен подписчик на шаблоны: склад.*, документы.*");
КонецПроцедуры
```

### Шаг 6: Отправьте тестовые логи

```bsl
&НаКлиенте
Процедура ОтправитьТестовыеЛоги(Команда)
    ОтправитьЛогПоТемеНаСервере("склад", "error", "Критическая ошибка склада");
    ОтправитьЛогПоТемеНаСервере("склад", "warning", "Предупреждение склада");
    ОтправитьЛогПоТемеНаСервере("документы", "info", "Документ создан");
    ОтправитьЛогПоТемеНаСервере("регламент", "error", "Ошибка регламента");
    ОтправитьЛогПоТемеНаСервере("обмен", "info", "Обмен завершён");
КонецПроцедуры
```

---

## Ожидаемые результаты

### Подписчик #1 (шаблон `#`):
Получит **все** сообщения:
```
[x] Получено: Критическая ошибка склада
[x] Получено: Предупреждение склада
[x] Получено: Документ создан
[x] Получено: Ошибка регламента
[x] Получено: Обмен завершён
```

### Подписчик #2 (шаблон `склад.#`):
Получит только сообщения, начинающиеся с `"склад."`:
```
[x] Получено: Критическая ошибка склада
[x] Получено: Предупреждение склада
```

### Подписчик #3 (шаблон `*.error`):
Получит только сообщения с уровнем `error`:
```
[x] Получено: Критическая ошибка склада
[x] Получено: Ошибка регламента
```

### Подписчик #4 (шаблоны `склад.*`, `документы.*`):
Получит сообщения из подсистем "склад" и "документы":
```
[x] Получено: Критическая ошибка склада
[x] Получено: Предупреждение склада
[x] Получено: Документ создан
```

---

## Практическое применение в 1С

### Сценарий 1: Система мониторинга по подсистемам

```bsl
// Структура routing_key: "<подсистема>.<уровень>"

// Подписчик 1: Все логи подсистемы "обмен"
Шаблоны1 = Новый Массив;
Шаблоны1.Добавить("обмен.#");

// Подписчик 2: Только критические ошибки всех подсистем
Шаблоны2 = Новый Массив;
Шаблоны2.Добавить("*.error");
Шаблоны2.Добавить("*.critical");

// Подписчик 3: Всё из "склад" и "продажи"
Шаблоны3 = Новый Массив;
Шаблоны3.Добавить("склад.#");
Шаблоны3.Добавить("продажи.#");
```

### Сценарий 2: Трёхуровневая система логирования

```bsl
// Структура routing_key: "<подсистема>.<документ>.<действие>"

// Примеры routing_key:
// "склад.поступление.создан"
// "склад.поступление.проведен"
// "склад.списание.отменен"
// "продажи.заказ.создан"

// Подписчик 1: Все действия с поступлениями
Шаблоны1 = Новый Массив;
Шаблоны1.Добавить("*.поступление.*");

// Подписчик 2: Все проводки документов склада
Шаблоны2 = Новый Массив;
Шаблоны2.Добавить("склад.*.проведен");

// Подписчик 3: Всё из подсистемы "склад"
Шаблоны3 = Новый Массив;
Шаблоны3.Добавить("склад.#");
```

### Сценарий 3: Уведомления с приоритетами и категориями

```bsl
// Структура routing_key: "<категория>.<тип>.<приоритет>"

// Примеры:
// "система.безопасность.высокий"
// "пользователь.задача.средний"
// "интеграция.обмен.низкий"

// Подписчик 1: Высокий приоритет любой категории → push-уведомления
Шаблоны1 = Новый Массив;
Шаблоны1.Добавить("*.*.высокий");

// Подписчик 2: Безопасность любого приоритета → логирование в БД
Шаблоны2 = Новый Массив;
Шаблоны2.Добавить("система.безопасность.#");

// Подписчик 3: Всё кроме низкого приоритета
Шаблоны3 = Новый Массив;
Шаблоны3.Добавить("*.*.высокий");
Шаблоны3.Добавить("*.*.средний");
```

### Сценарий 4: Гибкая маршрутизация отчётов

```bsl
// Структура routing_key: "<отдел>.<тип_отчета>.<период>"

// Примеры:
// "продажи.итоги.день"
// "продажи.итоги.месяц"
// "финансы.баланс.квартал"
// "склад.остатки.день"

// Подписчик 1: Все ежедневные отчёты
Шаблоны1 = Новый Массив;
Шаблоны1.Добавить("*.*.день");

// Подписчик 2: Все отчёты по продажам
Шаблоны2 = Новый Массив;
Шаблоны2.Добавить("продажи.#");

// Подписчик 3: Итоги продаж и финансов (любой период)
Шаблоны3 = Новый Массив;
Шаблоны3.Добавить("продажи.итоги.*");
Шаблоны3.Добавить("финансы.итоги.*");
```

---

## Важные замечания

### 1. Формат routing_key

Routing key должен быть списком слов, разделённых точками:
- ✅ `"склад.поступление.создан"`
- ✅ `"система.error"`
- ❌ `"складпоступление"` (нет разделителей)
- ❌ `"склад поступление"` (пробел вместо точки)

### 2. Символы подстановки работают только в binding_key

```bsl
// ✅ Правильно: шаблоны в binding_key (при подписке)
ШаблонПривязки = "склад.*";
КлиентRMQ.ОбъявитьОчередь(Канал, "", ШаблонПривязки, "topic_logs", ...);

// ❌ Неправильно: шаблоны в routing_key (при публикации)
КлючМаршрутизации = "склад.*"; // Это будет воспринято как БУКВАЛЬНАЯ строка "склад.*"
КлиентRMQ.ОпубликоватьСообщение(Канал, Данные, КлючМаршрутизации, ...);
```

### 3. Различие между `*` и `#`

| Символ | Заменяет | Пример | Совпадёт | Не совпадёт |
|--------|----------|--------|----------|-------------|
| `*` | Ровно одно слово | `склад.*.error` | `склад.поступление.error` | `склад.error`, `склад.товар.списание.error` |
| `#` | Ноль или более слов | `склад.#` | `склад.error`, `склад.поступление.error`, `склад.товар.списание.error` | `документы.error` |

### 4. Несколько привязок для одной очереди

Как и в Уроке 4, вы можете создать несколько привязок с разными шаблонами для одной очереди:

```bsl
// Первый вызов - создает очередь и первую привязку
Очередь = КлиентRMQ.ОбъявитьОчередь(Канал, "", "склад.#", "topic_logs", Ложь, Истина, Истина);
ИмяОчереди = Очередь.ИмяОчереди;

// Второй вызов - добавляет вторую привязку
КлиентRMQ.ОбъявитьОчередь(Канал, ИмяОчереди, "документы.#", "topic_logs", Ложь, Истина, Истина);

// Теперь очередь получит все сообщения из "склад" и "документы"
```

### 5. Сообщения без подходящих очередей отбрасываются

Если сообщение опубликовано с `routing_key`, который не совпадает ни с одним `binding_key`, оно будет **потеряно**.

### 6. Регистр учитывается

```bsl
// routing_key = "Склад.Error"
// binding_key = "склад.error"
// ❌ НЕ совпадёт (разный регистр)
```

### 7. Максимальная длина routing_key

Максимальная длина routing_key — **255 байт**. Учитывайте кодировку UTF-8 для русских символов (один символ = 2-3 байта).

---

## Сравнение типов обменников

| Тип | Маршрутизация | Когда использовать | Пример |
|-----|---------------|-------------------|--------|
| **fanout** | Всем привязанным очередям | Широковещательная рассылка | Уведомления всем пользователям |
| **direct** | Точное совпадение ключа | Категоризация по одному признаку | Логи по уровню важности |
| **topic** | Шаблоны с `*` и `#` | Категоризация по нескольким признакам | Логи по подсистеме + уровню |
| **headers** | По заголовкам сообщений | Сложная логика маршрутизации | Комбинация условий (не покрыто в уроках) |

---

## Что дальше?

Мы изучили все основные типы обменников для маршрутизации сообщений. Теперь пора перейти к более сложному паттерну — **удалённому вызову процедур (RPC)**.

В [Уроке 6](./Lesson6_ru.md) мы построим систему RPC с использованием RabbitMQ, где клиент сможет отправить запрос и получить ответ через очереди сообщений.

---

## Резюме

✅ **Изучили:**
- Обменник типа `topic` для гибкой маршрутизации
- Специальные символы `*` (одно слово) и `#` (ноль или более слов)
- Формат routing_key как список слов, разделённых точками
- Различие между routing_key (при публикации) и binding_key (при подписке)
- Универсальность topic exchange (может работать как fanout или direct)

✅ **Реализовали:**
- Издателя логов с многоуровневой категоризацией
- Подписчика с фильтрацией по шаблонам
- Систему с несколькими подписчиками на разные шаблоны

✅ **Научились:**
- Создавать сложные шаблоны маршрутизации для 1С
- Подписываться на сообщения по нескольким критериям
- Применять паттерн Topics для практических задач (логирование по подсистемам, отчёты, уведомления)

✅ **Практические сценарии для 1С:**
- Мониторинг по подсистемам и уровням важности
- Трёхуровневая система логирования (подсистема.документ.действие)
- Уведомления с приоритетами и категориями
- Гибкая маршрутизация отчётов

---

**Следующий урок:** [Урок 6 - RPC (Удалённый вызов процедур)](./Lesson6_ru.md) 🚀

