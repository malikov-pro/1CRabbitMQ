# Руководство RabbitMQ - Урок 6: "RPC" (Удалённый вызов процедур)

## Введение (Introduction)

### Предварительные требования

Убедитесь, что:
- ✅ RabbitMQ запущен (см. [DOCKER_README.md](./DOCKER_README.md))
- ✅ Компонента `КлиентRMQ` подключена к конфигурации
- ✅ Вы изучили все предыдущие уроки (особенно [Урок 2](./Lesson2_ru.md) о Work Queues)

### Чему посвящен этот урок

Во [втором уроке](./Lesson2_ru.md) мы узнали, как использовать _Work Queues_ (рабочие очереди) для распределения трудоёмких задач между несколькими работниками.

Но что, если нам нужно **запустить функцию на удалённом компьютере и дождаться результата**? Это уже совсем другая история. Этот паттерн широко известен как **_Remote Procedure Call_** или **_RPC_** (удалённый вызов процедур).

В этом уроке мы используем RabbitMQ для создания RPC-системы: **клиент** и **масштабируемый RPC-сервер**. Так как у нас нет реальных трудоёмких задач, мы создадим учебный RPC-сервис, который **вычисляет числа Фибоначчи**.

---

## Клиентский интерфейс

Чтобы проиллюстрировать, как можно использовать RPC-сервис, представим простой клиентский интерфейс. Он должен предоставлять метод, который **отправляет RPC-запрос и блокируется до получения ответа**:

```bsl
// Псевдокод - концепция
Результат = ВычислитьФибоначчиRPC(4);
Сообщить("fib(4) = " + Результат); // fib(4) = 3
```

---

## ⚠️ Примечание о RPC

> **Важно:** Хотя RPC — довольно распространённый паттерн в вычислительной технике, **его часто критикуют**. 
>
> Проблемы возникают, когда программист не осознаёт, является ли вызов функции **локальным** или это **медленный RPC**. Такая путаница приводит к непредсказуемости системы и излишней сложности при отладке. Вместо упрощения, неправильное использование RPC может превратить код в спагетти, который невозможно поддерживать.

### Рекомендации:

✅ **Делайте очевидным,** какой вызов функции локальный, а какой удалённый  
✅ **Документируйте систему** — чётко определяйте зависимости между компонентами  
✅ **Обрабатывайте ошибки** — как клиент должен реагировать, если RPC-сервер долго не работает?

> 💡 **Если сомневаетесь — избегайте RPC.** По возможности используйте **асинхронный pipeline** — вместо блокирующего RPC результаты асинхронно передаются на следующий этап обработки.

---

## Очередь обратного вызова (Callback Queue)

Паттерн запрос-ответ в RabbitMQ включает простое взаимодействие между сервером и клиентом:

1. **Клиент** отправляет сообщение-запрос
2. **Сервер** отвечает сообщением-ответом

Чтобы получить ответ, **клиент должен отправить имя очереди для ответа** вместе с запросом. Эта очередь часто создаётся сервером RabbitMQ с автогенерируемым именем, но может иметь и заранее определённое имя.

Сервер будет использовать это имя для отправки ответа через **обменник по умолчанию** (default exchange).

### Пример (концепция):

```bsl
// 1. Создаём эксклюзивную очередь для ответов (имя сгенерирует сервер)
ОчередьОтветов = КлиентRMQ.ОбъявитьОчередь(Канал, "", "", "", Ложь, Истина, Истина);
ИмяОчередиОтветов = ОчередьОтветов.ИмяОчереди; // например: "amq.gen-AbC123..."

// 2. Устанавливаем свойства сообщения
СвойстваСообщения = КлиентRMQ.НовыеСвойстваСообщения();
СвойстваСообщения.reply_to = ИмяОчередиОтветов;

// 3. Отправляем запрос
КлиентRMQ.ОпубликоватьСообщение(Канал, "30", "rpc_queue", "", Истина, Ложь, СвойстваСообщения);

// 4. Ждём ответ из очереди обратного вызова...
```

---

## Свойства сообщений (Message Properties)

Протокол AMQP 0-9-1 предопределяет набор из **14 свойств**, которые передаются вместе с сообщением. Большинство из них используются редко, за исключением:

| Свойство | Описание | Пример использования |
|----------|----------|---------------------|
| `delivery_mode` | Постоянство сообщения: `2` = persistent, другое = transient | Урок 2 — Durability |
| `content_type` | MIME-тип кодировки | `"application/json"` для JSON |
| `reply_to` | Имя очереди для ответа | RPC callback queue |
| `correlation_id` | Идентификатор для сопоставления ответа с запросом | Уникальный UUID для каждого RPC-вызова |

---

## Идентификатор корреляции (Correlation ID)

Создавать отдельную очередь обратного вызова для **каждого** RPC-запроса неэффективно. Лучший подход — создать **одну** очередь обратного вызова на клиента.

Но тогда возникает новая проблема: получив ответ в этой очереди, **непонятно, к какому запросу он относится**. Именно здесь используется свойство **`correlation_id`**.

### Как это работает:

1. Клиент устанавливает **уникальное значение** `correlation_id` для каждого запроса
2. Сервер копирует это значение в ответное сообщение
3. Клиент проверяет `correlation_id` в полученном ответе
4. Если `correlation_id` совпадает с ожидаемым — это наш ответ
5. Если `correlation_id` неизвестен — сообщение отбрасывается

### Почему игнорируем неизвестные сообщения?

Это связано с возможностью **race condition** на стороне сервера. Хотя это маловероятно, RPC-сервер может упасть **сразу после отправки ответа, но до подтверждения (ACK) запроса**. После перезапуска сервер обработает запрос снова, отправив дубликат ответа.

Поэтому клиент должен:
- ✅ Корректно обрабатывать дублирующиеся ответы
- ✅ RPC-операция должна быть **идемпотентной** (повторное выполнение даёт тот же результат)

---

## Схема работы RPC

```
   [Клиент]                                    [RPC-сервер]
       |                                              |
       | 1. Создаёт эксклюзивную очередь ответов     |
       |    (например: amq.gen-ABC123)                |
       |                                              |
       | 2. Генерирует уникальный correlation_id     |
       |    (например: uuid-12345)                    |
       |                                              |
       | 3. Отправляет запрос в "rpc_queue"          |
       |    - reply_to: amq.gen-ABC123                |
       |    - correlation_id: uuid-12345              |
       |    - body: "30" (вычислить fib(30))         |
       |--------------------------------------------->|
       |                                              |
       |                                              | 4. Получает запрос
       |                                              | 5. Вычисляет fib(30)
       |                                              | 6. Отправляет ответ:
       |                                              |    - в очередь "amq.gen-ABC123"
       |                                              |    - correlation_id: uuid-12345
       |<---------------------------------------------|    - body: "832040"
       |                                              |
       | 7. Получает ответ                           |
       | 8. Проверяет correlation_id                 |
       | 9. Возвращает результат пользователю        |
```

### Пошаговое описание:

1. При запуске **клиент** создаёт эксклюзивную **очередь обратного вызова**
2. Для RPC-запроса клиент отправляет сообщение с двумя свойствами:
   - `reply_to` — имя очереди для ответа
   - `correlation_id` — уникальное значение для этого запроса
3. Запрос отправляется в очередь **`rpc_queue`**
4. **RPC-сервер** (worker) ожидает запросы в этой очереди
5. Когда запрос появляется, сервер выполняет работу и отправляет результат клиенту в очередь из поля `reply_to`
6. **Клиент** ожидает данные в очереди обратного вызова
7. При появлении сообщения клиент проверяет `correlation_id`
8. Если `correlation_id` совпадает с ожидаемым — возвращает ответ приложению

---

## Код RPC-сервера

```bsl
// ⚠️ Этот код должен выполняться в РЕГЛАМЕНТНОМ ЗАДАНИИ!
// Сервер будет постоянно слушать очередь "rpc_queue" и обрабатывать запросы

&НаСервере
Процедура ЗапуститьRPCСерверНаСервере() Экспорт
    
    // 1. Подключение
    КлиентRMQ = Обработки.КлиентRMQ.Создать();
    Подключение = КлиентRMQ.ПодключитьсяКСерверу("amqp://rmuser:rmpassword@localhost:5672/%2f");
    
    Если КлиентRMQ.ЭтоОшибка(Подключение) Тогда
        ВызватьИсключение "Ошибка подключения к RabbitMQ";
    КонецЕсли;
    
    Попытка
        // 2. Создание канала с QoS = 1 (для равномерного распределения нагрузки)
        Канал = КлиентRMQ.СоздатьКанал(Подключение, Ложь, 1);
        
        // 3. Объявление очереди для запросов
        ИмяОчереди = "rpc_queue";
        КлиентRMQ.ОбъявитьОчередь(
            Канал,
            ИмяОчереди,  // Имя очереди для RPC-запросов
            "",          // КлючМаршрутизации
            "",          // ТочкаОбмена (используем default exchange)
            Ложь,        // durable = Ложь (для примера)
            Ложь,        // exclusive = Ложь
            Ложь         // auto_delete = Ложь
        );
        
        Сообщить(СтрШаблон(" [x] Ожидание RPC-запросов в очереди: %1", ИмяОчереди));
        
        // 4. Создание получателя (НЕ используем auto_ack, будем подтверждать вручную)
        Получатель = КлиентRMQ.СоздатьПолучателя(
            Канал, 
            ИмяОчереди,
            "",      // ТэгПотребителя (автоматический)
            Ложь     // no_ack = Ложь (подтверждаем вручную)
        );
        
        // 5. Цикл обработки запросов
        Пока КлиентRMQ.СледующееСообщение(Получатель) Цикл
            
            ДанныеСообщения = КлиентRMQ.ДанныеСообщения();
            
            Если КлиентRMQ.ЭтоОшибка(ДанныеСообщения) Тогда
                Прервать;
            КонецЕсли;
            
            Попытка
                // 6. Извлекаем данные запроса
                ЗапросЧисла = Число(ДанныеСообщения.Данные);
                
                Сообщить(СтрШаблон(" [.] fib(%1)", ЗапросЧисла));
                
                // 7. Вычисляем число Фибоначчи
                Результат = ВычислитьФибоначчи(ЗапросЧисла);
                
                // 8. Извлекаем свойства запроса
                reply_to = ДанныеСообщения.СвойстваСообщения.reply_to;
                correlation_id = ДанныеСообщения.СвойстваСообщения.correlation_id;
                
                // 9. Формируем ответ
                СвойстваОтвета = КлиентRMQ.НовыеСвойстваСообщения();
                СвойстваОтвета.correlation_id = correlation_id;
                
                // 10. Отправляем ответ в очередь, указанную в reply_to
                //     Используем default exchange (пустая строка) и reply_to как routing_key
                Ответ = КлиентRMQ.ОпубликоватьСообщение(
                    Канал, 
                    Строка(Результат),  // Результат вычислений
                    reply_to,           // КлючМаршрутизации = имя очереди для ответа
                    "",                 // ТочкаОбмена (default exchange)
                    Истина,             // ПодтверждениеНеобходимо
                    Ложь,               // СохранятьСообщение
                    СвойстваОтвета      // Свойства с correlation_id
                );
                
                Если НЕ КлиентRMQ.ЭтоОшибка(Ответ) Тогда
                    Сообщить(СтрШаблон(" [x] Ответ отправлен: fib(%1) = %2", ЗапросЧисла, Результат));
                КонецЕсли;
                
                // 11. Подтверждаем обработку запроса
                КлиентRMQ.ПодтвердитьСообщение(Канал, ДанныеСообщения.DeliveryTag);
                
            Исключение
                ТекстОшибки = ОписаниеОшибки();
                Сообщить(" [!] Ошибка обработки запроса: " + ТекстОшибки);
                
                // Отклоняем сообщение при ошибке
                КлиентRMQ.ОтклонитьСообщение(Канал, ДанныеСообщения.DeliveryTag, Ложь);
            КонецПопытки;
            
        КонецЦикла;
        
    Исключение
        ЗаписьЛогаСистемы("RabbitMQ.RPC", ОписаниеОшибки(), , , УровеньЛогаСистемы.Ошибка);
    КонецПопытки;
    
    // 12. Отключение
    КлиентRMQ.ОтключитьсяОтСервера(Подключение);
    
КонецПроцедуры

// Функция вычисления числа Фибоначчи (рекурсивная, неоптимальная — для примера)
&НаСервере
Функция ВычислитьФибоначчи(n)
    
    Если n = 0 Тогда
        Возврат 0;
    ИначеЕсли n = 1 Тогда
        Возврат 1;
    Иначе
        Возврат ВычислитьФибоначчи(n - 1) + ВычислитьФибоначчи(n - 2);
    КонецЕсли;
    
КонецФункции
```

### Пояснения к коду сервера:

1. **Подключение и настройка канала** с `QoS = 1` для равномерного распределения нагрузки между несколькими серверами
2. **Объявление очереди** `rpc_queue` для получения запросов
3. **Цикл обработки** — сервер постоянно ждёт новые запросы
4. **Извлечение `reply_to` и `correlation_id`** из свойств запроса
5. **Вычисление результата** (число Фибоначчи)
6. **Отправка ответа** в очередь `reply_to` с тем же `correlation_id`
7. **Подтверждение обработки** (ACK) только после успешной отправки ответа

---

## Код RPC-клиента

```bsl
// ⚠️ Этот код выполняется НА СЕРВЕРЕ через серверную процедуру

&НаСервере
Функция ВызватьRPCВычислениеФибоначчиНаСервере(ЧислоДляВычисления) Экспорт
    
    // 1. Подключение
    КлиентRMQ = Обработки.КлиентRMQ.Создать();
    Подключение = КлиентRMQ.ПодключитьсяКСерверу("amqp://rmuser:rmpassword@localhost:5672/%2f");
    
    Если КлиентRMQ.ЭтоОшибка(Подключение) Тогда
        ВызватьИсключение "Ошибка подключения к RabbitMQ";
    КонецЕсли;
    
    Попытка
        // 2. Создание канала
        Канал = КлиентRMQ.СоздатьКанал(Подключение);
        
        // 3. Создание эксклюзивной очереди для ответов
        //    Имя очереди пустое — RabbitMQ сгенерирует уникальное имя
        ОчередьОтветов = КлиентRMQ.ОбъявитьОчередь(
            Канал,
            "",      // Имя очереди (пустое — сервер сгенерирует)
            "",      // КлючМаршрутизации
            "",      // ТочкаОбмена
            Ложь,    // durable
            Истина,  // exclusive = Истина (эксклюзивная очередь)
            Истина   // auto_delete = Истина (удалится после закрытия канала)
        );
        
        ИмяОчередиОтветов = ОчередьОтветов.ИмяОчереди;
        Сообщить(СтрШаблон(" [*] Создана очередь для ответов: %1", ИмяОчередиОтветов));
        
        // 4. Генерация уникального correlation_id
        correlation_id = Строка(Новый УникальныйИдентификатор());
        
        // 5. Формирование свойств запроса
        СвойстваЗапроса = КлиентRMQ.НовыеСвойстваСообщения();
        СвойстваЗапроса.reply_to = ИмяОчередиОтветов;
        СвойстваЗапроса.correlation_id = correlation_id;
        
        // 6. Отправка запроса
        Сообщить(СтрШаблон(" [x] Запрос: fib(%1)", ЧислоДляВычисления));
        
        Ответ = КлиентRMQ.ОпубликоватьСообщение(
            Канал, 
            Строка(ЧислоДляВычисления),  // Данные запроса
            "rpc_queue",                 // КлючМаршрутизации = имя очереди запросов
            "",                          // ТочкаОбмена (default exchange)
            Истина,                      // ПодтверждениеНеобходимо
            Ложь,                        // СохранятьСообщение
            СвойстваЗапроса              // Свойства с reply_to и correlation_id
        );
        
        Если КлиентRMQ.ЭтоОшибка(Ответ) Тогда
            ВызватьИсключение "Ошибка отправки RPC-запроса: " + Ответ.Текст;
        КонецЕсли;
        
        // 7. Ожидание ответа
        Сообщить(" [*] Ожидание ответа...");
        
        РезультатВычисления = Неопределено;
        ТаймаутСекунд = 30;
        НачалоОжидания = ТекущаяДата();
        
        Пока Истина Цикл
            
            // Проверка таймаута
            Если ТекущаяДата() - НачалоОжидания > ТаймаутСекунд Тогда
                ВызватьИсключение "Таймаут ожидания ответа от RPC-сервера";
            КонецЕсли;
            
            // Попытка получить одно сообщение из очереди ответов
            ЕстьСообщение = КлиентRMQ.ОдноСледующееСообщение(Канал, ИмяОчередиОтветов);
            
            Если ЕстьСообщение Тогда
                
                ДанныеОтвета = КлиентRMQ.ДанныеСообщения();
                
                Если КлиентRMQ.ЭтоОшибка(ДанныеОтвета) Тогда
                    Прервать;
                КонецЕсли;
                
                // Проверяем correlation_id
                Если ДанныеОтвета.СвойстваСообщения.correlation_id = correlation_id Тогда
                    // Это наш ответ!
                    РезультатВычисления = Число(ДанныеОтвета.Данные);
                    Сообщить(СтрШаблон(" [✓] Получен ответ: %1", РезультатВычисления));
                    Прервать;
                Иначе
                    // Не наше сообщение — игнорируем
                    Сообщить(" [!] Получено сообщение с неизвестным correlation_id — игнорируем");
                КонецЕсли;
                
            Иначе
                // Сообщений пока нет — небольшая пауза перед следующей попыткой
                // В 1С нет аналога process_data_events, поэтому делаем паузу
                // ⚠️ В production лучше использовать фоновое задание с периодической проверкой
            КонецЕсли;
            
        КонецЦикла;
        
        // 8. Закрытие канала (эксклюзивная очередь удалится автоматически)
        КлиентRMQ.ОтключитьсяОтСервера(Подключение);
        
        Возврат РезультатВычисления;
        
    Исключение
        КлиентRMQ.ОтключитьсяОтСервера(Подключение);
        ВызватьИсключение;
    КонецПопытки;
    
КонецФункции

// Клиентская команда для вызова RPC
&НаКлиенте
Процедура ВычислитьЧерезRPC(Команда)
    
    ЧислоДляВычисления = 10; // Можно запросить у пользователя
    
    Результат = ВызватьRPCВычислениеФибоначчиНаСервере(ЧислоДляВычисления);
    
    Сообщить(СтрШаблон("Результат: fib(%1) = %2", ЧислоДляВычисления, Результат));
    
КонецПроцедуры
```

### Пояснения к коду клиента:

1. **Создание эксклюзивной очереди** для ответов с автогенерируемым именем
2. **Генерация уникального `correlation_id`** (используем `УникальныйИдентификатор`)
3. **Отправка запроса** с свойствами `reply_to` и `correlation_id`
4. **Цикл ожидания ответа** с проверкой `correlation_id`
5. **Таймаут** — если ответ не пришёл за 30 секунд, выбрасываем исключение
6. **Автоматическое удаление** эксклюзивной очереди при закрытии канала

---

## Запуск и тестирование

### Шаг 1: Запустите RPC-сервер

Создайте регламентное задание или запустите в фоновом задании:

```bsl
&НаКлиенте
Процедура ЗапуститьRPCСервер(Команда)
    
    ФоновыеЗадания.Выполнить(
        "ЗапуститьRPCСерверНаСервере", 
        Новый Массив, 
        , 
        "RPC-сервер: Фибоначчи"
    );
    
    Сообщить("RPC-сервер запущен в фоновом задании");
    
КонецПроцедуры
```

**Ожидаемый вывод:**
```
[x] Ожидание RPC-запросов в очереди: rpc_queue
```

### Шаг 2: Отправьте RPC-запрос от клиента

```bsl
&НаКлиенте
Процедура ВычислитьФибоначчи10(Команда)
    Результат = ВызватьRPCВычислениеФибоначчиНаСервере(10);
    Сообщить("fib(10) = " + Результат); // fib(10) = 55
КонецПроцедуры
```

**Ожидаемый вывод на клиенте:**
```
[*] Создана очередь для ответов: amq.gen-Jz3F2...
[x] Запрос: fib(10)
[*] Ожидание ответа...
[✓] Получен ответ: 55
Результат: fib(10) = 55
```

**Ожидаемый вывод на сервере:**
```
[.] fib(10)
[x] Ответ отправлен: fib(10) = 55
```

### Шаг 3: Запустите несколько RPC-серверов для масштабирования

```bsl
&НаКлиенте
Процедура ЗапуститьНесколькоRPCСерверов(Команда)
    
    // Запускаем 3 экземпляра RPC-сервера
    Для Индекс = 1 По 3 Цикл
        ФоновыеЗадания.Выполнить(
            "ЗапуститьRPCСерверНаСервере", 
            Новый Массив, 
            , 
            СтрШаблон("RPC-сервер #%1", Индекс)
        );
    КонецЦикла;
    
    Сообщить("Запущено 3 RPC-сервера");
    
КонецПроцедуры
```

Благодаря `QoS = 1` запросы будут **равномерно распределяться** между всеми серверами.

---

## Практическое применение в 1С

### Сценарий 1: Тяжёлые вычисления

```bsl
// Вместо Фибоначчи — реальная задача:
// - Расчёт себестоимости продукции
// - Построение сложного отчёта
// - Валидация большого количества документов

Функция РассчитатьСебестоимостьRPC(МассивНоменклатуры)
    // Отправляем массив на RPC-сервер
    // Сервер выполняет расчёт в фоне
    // Возвращает результат
КонецФункции
```

### Сценарий 2: Интеграция с внешними системами

```bsl
// RPC-сервер выполняет запросы к внешнему API:
// - Проверка контрагента в ЕГРЮЛ
// - Получение курсов валют
// - Отправка email/SMS

Функция ПолучитьДанныеЕГРЮЛ_RPC(ИНН)
    // RPC-сервер обращается к внешнему API
    // Возвращает данные о контрагенте
КонецФункции
```

### Сценарий 3: Распределённая обработка данных

```bsl
// Несколько RPC-серверов обрабатывают большой массив данных:
// - Импорт большого файла (разбивка на части)
// - Пакетная обработка документов
// - Генерация отчётов для филиалов

Функция ОбработатьПартиюДокументовRPC(МассивДокументов)
    // Отправляем на RPC-сервер
    // Несколько серверов обрабатывают параллельно
КонецФункции
```

---

## Важные замечания

### 1. Преимущества представленной реализации

✅ **Масштабируемость** — просто запустите ещё один RPC-сервер для увеличения производительности  
✅ **Эффективность** — клиенту нужна только одна сетевая операция для RPC-запроса  
✅ **Простота** — нет синхронных вызовов типа `queue_declare` для каждого запроса

### 2. Что НЕ решает этот пример

⚠️ **Отсутствие серверов** — как клиент должен реагировать, если RPC-серверы не запущены?  
⚠️ **Таймауты** — в примере простой таймаут, но нужна более гибкая стратегия  
⚠️ **Исключения сервера** — если сервер упал с ошибкой, клиент об этом не узнает  
⚠️ **Валидация входных данных** — нужно проверять границы и корректность данных

### 3. Рекомендации для production

#### Для клиента:

```bsl
// ✅ Добавьте обработку отсутствия серверов
Если РезультатВычисления = Неопределено Тогда
    ВызватьИсключение "RPC-сервер не отвечает. Попробуйте позже.";
КонецЕсли;

// ✅ Настраиваемый таймаут
ТаймаутСекунд = ПолучитьНастройкуRPCТаймаут(); // из константы или настроек

// ✅ Логирование всех RPC-вызовов
ЗаписьЛогаСистемы("RabbitMQ.RPC.Client", 
    СтрШаблон("Вызов: fib(%1)", ЧислоДляВычисления), 
    , , 
    УровеньЛогаСистемы.Информация);
```

#### Для сервера:

```bsl
// ✅ Обработка ошибок с возвратом информации клиенту
Попытка
    Результат = ВычислитьФибоначчи(ЗапросЧисла);
    ДанныеОтвета = Строка(Результат);
Исключение
    // Отправляем ошибку клиенту
    ДанныеОтвета = "ERROR: " + ОписаниеОшибки();
КонецПопытки;

// ✅ Валидация входных данных
Если ЗапросЧисла < 0 ИЛИ ЗапросЧисла > 40 Тогда
    ДанныеОтвета = "ERROR: Число должно быть от 0 до 40";
    // Отправить ошибку клиенту
КонецЕсли;

// ✅ Мониторинг производительности
НачалоОбработки = ТекущаяУниверсальнаяДатаВМиллисекундах();
Результат = ВычислитьФибоначчи(ЗапросЧисла);
ВремяОбработкиМс = ТекущаяУниверсальнаяДатаВМиллисекундах() - НачалоОбработки;

Если ВремяОбработкиМс > 5000 Тогда
    Сообщить(СтрШаблон(" [!] Медленная обработка: %1 мс", ВремяОбработкиМс));
КонецЕсли;
```

### 4. Идемпотентность

RPC-операции должны быть **идемпотентными** — повторное выполнение даёт тот же результат:

```bsl
// ✅ Идемпотентная операция
Функция ПолучитьИнформациюОКонтрагенте(ИНН)
    // Можно вызывать многократно — результат не меняется
КонецФункции

// ❌ НЕ идемпотентная операция
Процедура СоздатьДокумент()
    // Повторный вызов создаст дубликат документа!
    // Нужно добавить проверку на существование
КонецПроцедуры
```

### 5. Использование в 1С

В 1С:Предприятие компонента работает только на сервере, поэтому:

- ✅ **RPC-сервер** — запускается в регламентном задании
- ✅ **RPC-клиент** — вызывается через серверную процедуру (`&НаСервере`)
- ⚠️ **Блокирующие вызовы** — клиентский код будет заблокирован до получения ответа
- 💡 **Альтернатива** — для длительных операций лучше использовать асинхронный подход (фоновые задания)

---

## Сравнение с Work Queues

| Характеристика | Work Queues (Урок 2) | RPC (Урок 6) |
|----------------|---------------------|--------------|
| **Направление** | Односторонняя отправка | Запрос-ответ |
| **Ожидание результата** | Нет | Да (блокирующий вызов) |
| **Ответ** | Нет | Через callback queue |
| **Использование** | Фоновая обработка | Синхронные вычисления |
| **Пример** | Отправка email, обработка файлов | Валидация данных, расчёты |

---

## Что дальше?

Мы изучили паттерн RPC — синхронного взаимодействия через RabbitMQ. Но что, если мы хотим **гарантировать доставку сообщения** в условиях сбоев сети или падения брокера?

В [Уроке 7](./Lesson7_ru.md) мы изучим **Publisher Confirms** — механизм подтверждения доставки сообщений от брокера к издателю.

---

## Резюме

✅ **Изучили:**
- Паттерн RPC (Remote Procedure Call) для синхронного взаимодействия
- Концепцию callback queue (очередь обратного вызова)
- Использование свойств `reply_to` и `correlation_id`
- Обработку запросов и отправку ответов

✅ **Реализовали:**
- RPC-сервер для вычисления чисел Фибоначчи
- RPC-клиент с ожиданием ответа и таймаутом
- Масштабируемую систему с несколькими серверами

✅ **Научились:**
- Создавать эксклюзивные очереди для ответов
- Использовать correlation_id для сопоставления запросов и ответов
- Обрабатывать ошибки и таймауты
- Применять RPC для практических задач в 1С

✅ **Понимаем ограничения:**
- RPC — не серебряная пуля, нужно применять осознанно
- Важна идемпотентность операций
- Необходима обработка ошибок и таймаутов
- Для долгих операций лучше использовать асинхронный подход

---

**Следующий урок:** [Урок 7 - Publisher Confirms (Подтверждения публикации)](./Lesson7_ru.md) 🚀

